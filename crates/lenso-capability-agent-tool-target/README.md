# Lenso Agent Tool Target Capability

Host-private routing contract used by the Console Agent Tool proxy. The Host owns target Agent discovery and transport; the Plugin sees only Agent identity, generation-fenced catalogs, and execution outcomes.
