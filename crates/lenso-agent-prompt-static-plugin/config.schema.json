{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["contributions"],
  "properties": {
    "contributions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "version", "kind", "content"],
        "properties": {
          "id": { "type": "string" },
          "version": { "type": "string" },
          "kind": { "type": "string", "enum": ["instruction", "skill"] },
          "content": { "type": "string" }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
