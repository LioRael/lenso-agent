# Explain the retry policy without changing the Workspace

Identify the configured retry budget and the total attempts per job. The task is
analysis only: the Workspace must remain byte-for-byte unchanged.
