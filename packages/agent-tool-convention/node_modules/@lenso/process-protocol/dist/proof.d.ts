import type { HandshakeParams } from "./types.js";
import { type InitializeParams } from "./authoring.js";
/** RFC 8785-compatible canonical JSON for the restricted proof value profile. */
export declare function canonicalizeProofValue(value: unknown): Uint8Array;
/** Canonical bytes hashed to obtain `handshake_params_digest`. */
export declare function handshakeProofPayload(params: HandshakeParams): Uint8Array;
/** Exact bytes authenticated by `host_proof`. */
export declare function hostProofMessage(handshakeDigest: Uint8Array): Uint8Array;
/** Exact bytes authenticated by `child_proof`. */
export declare function childProofMessage(handshakeDigest: Uint8Array, session: string): Uint8Array;
export interface AuthoringHandshakeProofInput {
    readonly initialize: InitializeParams;
    readonly callback_origin: string;
    readonly host_nonce: string;
}
/** Canonical bytes hashed before authenticating one Authoring V2 initialization. */
export declare function authoringHandshakeProofPayload(input: AuthoringHandshakeProofInput): Uint8Array;
/** Exact bytes authenticated by the Authoring V2 Host proof. */
export declare function authoringHostProofMessage(handshakeDigest: Uint8Array): Uint8Array;
/** Exact bytes authenticated by the Authoring V2 child proof. */
export declare function authoringChildProofMessage(handshakeDigest: Uint8Array, childNonce: string): Uint8Array;
/** Exact bytes authenticating one child-to-Host callback request. */
export declare function authoringCallbackProofMessage(session: string, method: "lenso.call" | "lenso.event.publish" | "lenso.stream.open" | "lenso.stream.send" | "lenso.stream.receive" | "lenso.stream.close_send" | "lenso.stream.cancel" | "lenso.settled", params: unknown): Uint8Array;
/** Decodes one canonical unpadded base64url value containing exactly 32 bytes. */
export declare function decodeBase64Url32(value: string, name?: string): Uint8Array;
/** Encodes bytes as canonical unpadded base64url. */
export declare function encodeBase64Url(value: Uint8Array): string;
