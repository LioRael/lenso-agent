var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __hasOwnProp = Object.prototype.hasOwnProperty;
function __accessProp(key) {
  return this[key];
}
var __toCommonJS = (from) => {
  var entry = (__moduleCache ??= new WeakMap).get(from), desc;
  if (entry)
    return entry;
  entry = __defProp({}, "__esModule", { value: true });
  if (from && typeof from === "object" || typeof from === "function") {
    for (var key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(entry, key))
        __defProp(entry, key, {
          get: __accessProp.bind(from, key),
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
        });
  }
  __moduleCache.set(from, entry);
  return entry;
};
var __moduleCache;
var __returnValue = (v) => v;
function __exportSetter(name, newValue) {
  this[name] = __returnValue.bind(null, newValue);
}
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {
      get: all[name],
      enumerable: true,
      configurable: true,
      set: __exportSetter.bind(all, name)
    });
};

// src/index.ts
var exports_src = {};
__export(exports_src, {
  AUTHORING_API_VERSION: () => AUTHORING_API_VERSION,
  CANCEL_METHOD: () => CANCEL_METHOD,
  DEFAULT_MAX_FRAME_BYTES: () => DEFAULT_MAX_FRAME_BYTES,
  HANDSHAKE_METHOD: () => HANDSHAKE_METHOD,
  PROCESS_PROFILE: () => PROCESS_PROFILE,
  PROVIDE_REQUEST_PROFILE: () => PROVIDE_REQUEST_PROFILE,
  REQUEST_METHOD: () => REQUEST_METHOD,
  SHUTDOWN_METHOD: () => SHUTDOWN_METHOD,
  VALUE_PROFILE: () => VALUE_PROFILE,
  authoringCallbackProofMessage: () => authoringCallbackProofMessage,
  authoringChildProofMessage: () => authoringChildProofMessage,
  authoringHandshakeProofPayload: () => authoringHandshakeProofPayload,
  authoringHostProofMessage: () => authoringHostProofMessage,
  canonicalizeProofValue: () => canonicalizeProofValue,
  childProofMessage: () => childProofMessage,
  decodeBase64Url32: () => decodeBase64Url32,
  defaultAuthoringLimits: () => defaultAuthoringLimits,
  encodeBase64Url: () => encodeBase64Url,
  handshakeProofPayload: () => handshakeProofPayload,
  hostProofMessage: () => hostProofMessage,
  parseAuthoringFrame: () => parseAuthoringFrame,
  parseStrictJson: () => parseStrictJson,
  validateAuthoringMessage: () => validateAuthoringMessage,
  validateCancelParams: () => validateCancelParams,
  validateChildScope: () => validateChildScope,
  validateControlAck: () => validateControlAck,
  validateEventPublish: () => validateEventPublish,
  validateEventPublishResultFor: () => validateEventPublishResultFor,
  validateExtension: () => validateExtension,
  validateHandshakeIdentity: () => validateHandshakeIdentity,
  validateHandshakeParams: () => validateHandshakeParams,
  validateHandshakeResult: () => validateHandshakeResult,
  validateInitialize: () => validateInitialize,
  validateInitializeForRuntimeProfile: () => validateInitializeForRuntimeProfile,
  validateInitialized: () => validateInitialized,
  validateInvocationScope: () => validateInvocationScope,
  validateInvoke: () => validateInvoke,
  validateJsonRpcError: () => validateJsonRpcError,
  validateJsonRpcRequest: () => validateJsonRpcRequest,
  validateJsonRpcSuccess: () => validateJsonRpcSuccess,
  validateOutboundCall: () => validateOutboundCall,
  validateOutboundEventPublish: () => validateOutboundEventPublish,
  validateOutboundStreamOpen: () => validateOutboundStreamOpen,
  validateReadinessRecord: () => validateReadinessRecord,
  validateRequestParams: () => validateRequestParams,
  validateRequestResult: () => validateRequestResult,
  validateResultFor: () => validateResultFor,
  validateShutdownParams: () => validateShutdownParams,
  validateStreamAction: () => validateStreamAction,
  validateStreamOpen: () => validateStreamOpen,
  validateStreamOpenResultFor: () => validateStreamOpenResultFor,
  validateStreamResultFor: () => validateStreamResultFor
});
module.exports = __toCommonJS(exports_src);

// src/types.ts
var PROCESS_PROFILE = "lenso-process-jsonrpc-http-v1";
var VALUE_PROFILE = "lenso-json-value-v1";
var PROVIDE_REQUEST_PROFILE = "provide-request-v1";
var HANDSHAKE_METHOD = "lenso.process.v1.handshake";
var REQUEST_METHOD = "lenso.process.v1.request";
var CANCEL_METHOD = "lenso.process.v1.cancel";
var SHUTDOWN_METHOD = "lenso.process.v1.shutdown";

// src/validate.ts
var DIGEST = /^sha256:[0-9a-f]{64}$/;
var DECIMAL = /^(?:0|[1-9][0-9]*)$/;
var TOKEN = /^[A-Za-z0-9._@/:-]{1,256}$/;
var MAX_STRICT_JSON_NESTING = 127;
function parseStrictJson(wire) {
  try {
    new StrictJsonScanner(wire).scan();
    return JSON.parse(wire);
  } catch {
    throw new Error("invalid strict JSON");
  }
}
function validateReadinessRecord(value) {
  const readiness = record(value, "readiness record");
  exactKeys(readiness, ["protocol", "data_port", "control_port"]);
  if (readiness.protocol !== PROCESS_PROFILE) {
    throw new Error("unsupported Process Protocol profile");
  }
  const dataPort = port(readiness.data_port, "data_port");
  const controlPort = port(readiness.control_port, "control_port");
  if (dataPort === controlPort)
    throw new Error("readiness listeners must be distinct");
}
function validateHandshakeIdentity(value) {
  const identity = record(value, "handshake identity");
  exactKeys(identity, [
    "protocol_profile",
    "value_profile",
    "plugin_instance",
    "plugin_generation",
    "generation_spec_digest",
    "artifact_digest",
    "effective_host_grant_set_digest",
    "interaction_profiles",
    "provided_capabilities",
    "outbound_bindings",
    "peer_limits"
  ]);
  if (identity.protocol_profile !== PROCESS_PROFILE) {
    throw new Error("unsupported Process Protocol profile");
  }
  if (identity.value_profile !== VALUE_PROFILE) {
    throw new Error("unsupported portable value profile");
  }
  token(identity.plugin_instance, "plugin_instance");
  decimal(identity.plugin_generation, "plugin_generation");
  digest(identity.generation_spec_digest, "generation_spec_digest");
  digest(identity.artifact_digest, "artifact_digest");
  digest(identity.effective_host_grant_set_digest, "effective_host_grant_set_digest");
  const profiles = stringArray(identity.interaction_profiles, "interaction_profiles");
  sortedUnique(profiles, "interaction_profiles", (profile) => profile);
  if (profiles.length !== 1 || profiles[0] !== PROVIDE_REQUEST_PROFILE) {
    throw new Error("V1 requires exactly the provide-request-v1 interaction profile");
  }
  if (!Array.isArray(identity.provided_capabilities) || identity.provided_capabilities.length === 0) {
    throw new Error("at least one provided Capability is required");
  }
  for (const descriptor of identity.provided_capabilities) {
    validateCapabilityDescriptor(descriptor);
  }
  sortedUnique(identity.provided_capabilities, "provided_capabilities", (descriptor) => descriptor.capability_id);
  if (!Array.isArray(identity.outbound_bindings)) {
    throw new Error("outbound_bindings must be an array");
  }
  if (identity.outbound_bindings.length !== 0) {
    throw new Error("outbound bindings require a selected consume profile");
  }
  validatePeerLimits(identity.peer_limits);
}
function validateHandshakeParams(value) {
  const params = record(value, "handshake params");
  exactKeys(params, ["identity", "host_nonce", "host_proof"]);
  validateHandshakeIdentity(params.identity);
  decodeBase64Url32(string(params.host_nonce, "host_nonce"), "host_nonce");
  decodeBase64Url32(string(params.host_proof, "host_proof"), "host_proof");
}
function validateHandshakeResult(value, expected) {
  const result = record(value, "handshake result");
  exactKeys(result, ["identity", "session", "child_proof"]);
  validateHandshakeIdentity(result.identity);
  if (!sameIdentity(result.identity, expected)) {
    throw new Error("child handshake identity mismatch");
  }
  decodeBase64Url32(string(result.session, "session"), "session");
  decodeBase64Url32(string(result.child_proof, "child_proof"), "child_proof");
}
function validateRequestParams(value) {
  const params = record(value, "request params");
  exactKeys(params, [
    "session",
    "correlation_id",
    "capability_id",
    "descriptor_version",
    "descriptor_digest",
    "operation",
    "interaction",
    "caller_instance",
    "remaining_timeout_nanos",
    "extensions",
    "payload"
  ]);
  decodeBase64Url32(string(params.session, "session"), "session");
  decimal(params.correlation_id, "correlation_id");
  token(params.capability_id, "capability_id");
  token(params.descriptor_version, "descriptor_version");
  digest(params.descriptor_digest, "descriptor_digest");
  token(params.operation, "operation");
  if (params.interaction !== "request")
    throw new Error("interaction must be request");
  nullableToken(params.caller_instance, "caller_instance");
  nullableDecimal(params.remaining_timeout_nanos, "remaining_timeout_nanos");
  if (!Array.isArray(params.extensions))
    throw new Error("extensions must be an array");
  for (const extension of params.extensions)
    validateExtension(extension);
  sortedUnique(params.extensions, "extensions", (extension) => extension.key);
}
function validateRequestResult(value) {
  const result = record(value, "request result");
  exactKeys(result, ["session", "correlation_id", "outcome"]);
  decodeBase64Url32(string(result.session, "session"), "session");
  decimal(result.correlation_id, "correlation_id");
  const outcome = record(result.outcome, "request outcome");
  const kind = string(outcome.kind, "outcome kind");
  if (kind === "success") {
    exactKeys(outcome, ["kind", "value"]);
    return;
  }
  if (kind === "domain") {
    exactKeys(outcome, ["kind", "error"]);
    return;
  }
  if (kind !== "runtime")
    throw new Error("unknown request outcome kind");
  exactKeys(outcome, ["kind", "failure"]);
  const failure = record(outcome.failure, "child Runtime Failure");
  const failureKind = string(failure.kind, "child Runtime Failure kind");
  if (failureKind === "resource_exhausted") {
    exactKeys(failure, ["kind", "operation"]);
    token(failure.operation, "operation");
    return;
  }
  if (failureKind === "plugin_failure") {
    exactKeys(failure, ["kind", "detail"]);
    const detail = string(failure.detail, "Plugin Failure detail");
    if (detail.length === 0 || new TextEncoder().encode(detail).length > 1024) {
      throw new Error("Plugin Failure detail must contain 1..=1024 bytes");
    }
    return;
  }
  throw new Error("child Runtime Failure kind is host-authoritative or unknown");
}
function validateCancelParams(value) {
  const params = record(value, "cancel params");
  exactKeys(params, ["session", "correlation_id"]);
  decodeBase64Url32(string(params.session, "session"), "session");
  decimal(params.correlation_id, "correlation_id");
}
function validateShutdownParams(value) {
  const params = record(value, "shutdown params");
  exactKeys(params, ["session"]);
  decodeBase64Url32(string(params.session, "session"), "session");
}
function validateControlAck(value) {
  const result = record(value, "control acknowledgement");
  exactKeys(result, ["session", "accepted"]);
  decodeBase64Url32(string(result.session, "session"), "session");
  if (result.accepted !== true)
    throw new Error("control acknowledgement must be accepted");
}
function validateJsonRpcRequest(value, expectedMethod, validateParams) {
  const envelope = record(value, "JSON-RPC request");
  exactKeys(envelope, ["jsonrpc", "id", "method", "params"]);
  if (envelope.jsonrpc !== "2.0")
    throw new Error("jsonrpc must equal 2.0");
  decimal(envelope.id, "JSON-RPC id");
  if (envelope.method !== expectedMethod)
    throw new Error("unexpected JSON-RPC method");
  validateParams(envelope.params);
}
function validateJsonRpcSuccess(value, expectedId, validateResult) {
  const envelope = record(value, "JSON-RPC success");
  exactKeys(envelope, ["jsonrpc", "id", "result"]);
  if (envelope.jsonrpc !== "2.0")
    throw new Error("jsonrpc must equal 2.0");
  decimal(envelope.id, "JSON-RPC id");
  if (envelope.id !== expectedId)
    throw new Error("JSON-RPC response id mismatch");
  validateResult(envelope.result);
}
function validateJsonRpcError(value) {
  const envelope = record(value, "JSON-RPC error");
  exactKeys(envelope, ["jsonrpc", "id", "error"]);
  if (envelope.jsonrpc !== "2.0")
    throw new Error("jsonrpc must equal 2.0");
  if (envelope.id !== null)
    decimal(envelope.id, "JSON-RPC id");
  const error = record(envelope.error, "JSON-RPC error object");
  exactKeys(error, ["code", "message"]);
  if (![-32700, -32600, -32601, -32602, -32603].includes(Number(error.code))) {
    throw new Error("unsupported JSON-RPC error code");
  }
  const message = string(error.message, "JSON-RPC error message");
  const length = new TextEncoder().encode(message).length;
  if (length === 0 || length > 256) {
    throw new Error("JSON-RPC error message must contain 1..=256 bytes");
  }
}
function sameIdentity(left, right) {
  const zero = encodeBase64Url(new Uint8Array(32));
  const leftBytes = handshakeProofPayload({ identity: left, host_nonce: zero, host_proof: zero });
  const rightBytes = handshakeProofPayload({ identity: right, host_nonce: zero, host_proof: zero });
  return leftBytes.length === rightBytes.length && leftBytes.every((byte, index) => byte === rightBytes[index]);
}
function validateCapabilityDescriptor(value) {
  const descriptor = record(value, "Capability Descriptor");
  exactKeys(descriptor, [
    "capability_id",
    "descriptor_version",
    "descriptor_digest",
    "operations"
  ]);
  token(descriptor.capability_id, "capability_id");
  token(descriptor.descriptor_version, "descriptor_version");
  digest(descriptor.descriptor_digest, "descriptor_digest");
  if (!Array.isArray(descriptor.operations) || descriptor.operations.length === 0) {
    throw new Error("provided Capability operations cannot be empty");
  }
  for (const value2 of descriptor.operations) {
    const operation = record(value2, "operation");
    exactKeys(operation, ["operation", "interaction"]);
    token(operation.operation, "operation");
    if (operation.interaction !== "request") {
      throw new Error("V1 provided operation interaction must be request");
    }
  }
  sortedUnique(descriptor.operations, "operations", (operation) => operation.operation);
}
function validatePeerLimits(value) {
  const limits = record(value, "peer limits");
  const maxima = {
    max_http_body_bytes: 1048576,
    max_control_http_body_bytes: 65536,
    max_concurrent_requests: 256,
    child_request_queue_capacity: 1024,
    max_retired_correlation_ids: 1048576,
    control_queue_capacity: 256
  };
  exactKeys(limits, Object.keys(maxima));
  for (const [name, maximum] of Object.entries(maxima)) {
    const candidate = limits[name];
    if (!Number.isSafeInteger(candidate) || Number(candidate) <= 0 || Number(candidate) > maximum) {
      throw new Error(`${name} must be within 1..=${maximum}`);
    }
  }
}
function validateExtension(value) {
  const extension = record(value, "Invocation Extension");
  exactKeys(extension, ["key", "value"], ["issuer", "audience", "proof", "sealed"]);
  token(extension.key, "extension key");
  canonicalPaddedBase64(extension.value, "extension value");
  if (extension.issuer !== undefined)
    string(extension.issuer, "extension issuer");
  if (extension.audience !== undefined) {
    const audience = stringArray(extension.audience, "extension audience");
    sortedUnique(audience, "extension audience", (entry) => entry);
  }
  if (extension.proof !== undefined)
    string(extension.proof, "extension proof");
  if (extension.sealed !== undefined && typeof extension.sealed !== "boolean") {
    throw new Error("extension sealed must be boolean");
  }
}
function record(value, name) {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    throw new Error(`${name} must be an object`);
  }
  return value;
}
function exactKeys(value, required, optional = []) {
  const allowed = new Set([...required, ...optional]);
  for (const key of Object.keys(value)) {
    if (!allowed.has(key))
      throw new Error(`unknown field ${key}`);
  }
  for (const key of required) {
    if (!Object.hasOwn(value, key))
      throw new Error(`missing field ${key}`);
  }
}
function string(value, name) {
  if (typeof value !== "string")
    throw new Error(`${name} must be a string`);
  return value;
}
function stringArray(value, name) {
  if (!Array.isArray(value) || value.some((entry) => typeof entry !== "string")) {
    throw new Error(`${name} must be a string array`);
  }
  return value;
}
function token(value, name) {
  if (typeof value !== "string" || !TOKEN.test(value)) {
    throw new Error(`${name} must be a 1..=256 byte portable token`);
  }
}
function decimal(value, name) {
  if (typeof value !== "string" || !DECIMAL.test(value)) {
    throw new Error(`${name} must be a canonical non-negative decimal string`);
  }
  try {
    const parsed = BigInt(value);
    if (parsed > 18446744073709551615n)
      throw new Error;
  } catch {
    throw new Error(`${name} exceeds the unsigned 64-bit decimal profile`);
  }
}
function nullableToken(value, name) {
  if (value !== null)
    token(value, name);
}
function nullableDecimal(value, name) {
  if (value !== null)
    decimal(value, name);
}
function port(value, name) {
  if (!Number.isInteger(value) || Number(value) < 1 || Number(value) > 65535) {
    throw new Error(`${name} must be a non-zero TCP port`);
  }
  return Number(value);
}
function digest(value, name) {
  if (typeof value !== "string" || !DIGEST.test(value)) {
    throw new Error(`${name} must be a canonical SHA-256 digest`);
  }
}
function sortedUnique(values, name, key) {
  for (let index = 1;index < values.length; index += 1) {
    const previous = key(values[index - 1]);
    const current = key(values[index]);
    if (!(previous < current)) {
      throw new Error(`${name} must be strictly canonical-sorted and unique`);
    }
  }
}
function canonicalPaddedBase64(value, name) {
  if (typeof value !== "string" || value.length % 4 !== 0) {
    throw new Error(`${name} must be canonical padded Base64`);
  }
  try {
    const decoded = atob(value);
    if (btoa(decoded) !== value)
      throw new Error;
  } catch {
    throw new Error(`${name} must be canonical padded Base64`);
  }
}

class StrictJsonScanner {
  wire;
  index = 0;
  constructor(wire) {
    this.wire = wire;
  }
  scan() {
    this.whitespace();
    this.value(0);
    this.whitespace();
    if (this.index !== this.wire.length)
      throw new Error("trailing JSON input");
  }
  value(depth) {
    this.whitespace();
    const candidate = this.wire[this.index];
    if (candidate === "{") {
      if (depth >= MAX_STRICT_JSON_NESTING)
        throw new Error("JSON nesting is too deep");
      return this.object(depth + 1);
    }
    if (candidate === "[") {
      if (depth >= MAX_STRICT_JSON_NESTING)
        throw new Error("JSON nesting is too deep");
      return this.array(depth + 1);
    }
    if (candidate === '"') {
      this.string();
      return;
    }
    if (candidate === "t")
      return this.literal("true");
    if (candidate === "f")
      return this.literal("false");
    if (candidate === "n")
      return this.literal("null");
    this.number();
  }
  object(depth) {
    this.index += 1;
    this.whitespace();
    const keys = new Set;
    if (this.wire[this.index] === "}") {
      this.index += 1;
      return;
    }
    while (true) {
      const key = this.string();
      if (keys.has(key))
        throw new Error("duplicate object key");
      keys.add(key);
      this.whitespace();
      this.expect(":");
      this.value(depth);
      this.whitespace();
      if (this.wire[this.index] === "}") {
        this.index += 1;
        return;
      }
      this.expect(",");
      this.whitespace();
    }
  }
  array(depth) {
    this.index += 1;
    this.whitespace();
    if (this.wire[this.index] === "]") {
      this.index += 1;
      return;
    }
    while (true) {
      this.value(depth);
      this.whitespace();
      if (this.wire[this.index] === "]") {
        this.index += 1;
        return;
      }
      this.expect(",");
      this.whitespace();
    }
  }
  string() {
    if (this.wire[this.index] !== '"')
      throw new Error("expected JSON string");
    const start = this.index;
    this.index += 1;
    while (this.index < this.wire.length) {
      const character = this.wire[this.index];
      if (character === '"') {
        this.index += 1;
        return JSON.parse(this.wire.slice(start, this.index));
      }
      if (character === "\\") {
        this.index += 1;
        const escape = this.wire[this.index];
        if (escape === "u") {
          const hex = this.wire.slice(this.index + 1, this.index + 5);
          if (!/^[0-9A-Fa-f]{4}$/.test(hex))
            throw new Error("invalid JSON unicode escape");
          const codeUnit2 = Number.parseInt(hex, 16);
          if (codeUnit2 >= 55296 && codeUnit2 <= 56319) {
            if (this.wire[this.index + 5] !== "\\" || this.wire[this.index + 6] !== "u") {
              throw new Error("unpaired JSON surrogate");
            }
            const lowHex = this.wire.slice(this.index + 7, this.index + 11);
            if (!/^[0-9A-Fa-f]{4}$/.test(lowHex)) {
              throw new Error("invalid JSON unicode escape");
            }
            const lowCodeUnit = Number.parseInt(lowHex, 16);
            if (lowCodeUnit < 56320 || lowCodeUnit > 57343) {
              throw new Error("unpaired JSON surrogate");
            }
            this.index += 11;
            continue;
          }
          if (codeUnit2 >= 56320 && codeUnit2 <= 57343) {
            throw new Error("unpaired JSON surrogate");
          }
          this.index += 5;
          continue;
        }
        if (!escape || !'"\\/bfnrt'.includes(escape))
          throw new Error("invalid JSON escape");
        this.index += 1;
        continue;
      }
      if (character.charCodeAt(0) < 32)
        throw new Error("unescaped JSON control character");
      const codeUnit = character.charCodeAt(0);
      if (codeUnit >= 55296 && codeUnit <= 56319) {
        const lowCodeUnit = this.wire.charCodeAt(this.index + 1);
        if (lowCodeUnit < 56320 || lowCodeUnit > 57343) {
          throw new Error("unpaired JSON surrogate");
        }
        this.index += 2;
        continue;
      }
      if (codeUnit >= 56320 && codeUnit <= 57343) {
        throw new Error("unpaired JSON surrogate");
      }
      this.index += 1;
    }
    throw new Error("unterminated JSON string");
  }
  number() {
    const match = /^-?(?:0|[1-9][0-9]*)(?:\.[0-9]+)?(?:[eE][+-]?[0-9]+)?/.exec(this.wire.slice(this.index));
    if (!match)
      throw new Error("invalid JSON value");
    const lexeme = match[0];
    if (!Number.isFinite(Number(lexeme)))
      throw new Error("JSON number is out of range");
    this.index += lexeme.length;
  }
  literal(value) {
    if (!this.wire.startsWith(value, this.index))
      throw new Error("invalid JSON literal");
    this.index += value.length;
  }
  whitespace() {
    while (/[\t\n\r ]/.test(this.wire[this.index] ?? ""))
      this.index += 1;
  }
  expect(value) {
    if (this.wire[this.index] !== value)
      throw new Error(`expected ${value}`);
    this.index += 1;
  }
}

// src/authoring.ts
var AUTHORING_API_VERSION = 2;
var DEFAULT_MAX_FRAME_BYTES = 1048576;
var TOKEN2 = /^[A-Za-z0-9._@/:-]{1,256}$/;
var DECIMAL2 = /^(?:0|[1-9][0-9]*)$/;
var DIGEST2 = /^sha256:[0-9a-f]{64}$/;
var REQUIREMENT_ID = /^[a-z][a-z0-9_]{0,63}$/;
function defaultAuthoringLimits() {
  return {
    max_frame_bytes: DEFAULT_MAX_FRAME_BYTES,
    max_active_invocations: 32,
    max_active_outbound_calls: 32,
    max_queued_calls: 32,
    max_unfinished_executions: 32,
    max_retired_ids: 1024
  };
}
function validateInitialize(value) {
  const input = object(value, "initialize");
  exact(input, [
    "api_version",
    "identity",
    "config",
    "required_declarations",
    "routes",
    "provided_endpoints",
    "limits"
  ]);
  if (input.api_version !== AUTHORING_API_VERSION)
    fail("unsupported Authoring API version");
  validateIdentity(input.identity);
  validateLimits(input.limits);
  const declarations = array(input.required_declarations, "required_declarations");
  const routes = array(input.routes, "routes");
  const endpoints = array(input.provided_endpoints, "provided_endpoints");
  const byId = new Map;
  let previous = "";
  for (const candidate of declarations) {
    const declaration = object(candidate, "requirement declaration");
    exact(declaration, [
      "requirement_id",
      "capability_id",
      "descriptor_version",
      "descriptor_digest",
      "cardinality"
    ]);
    descriptor(declaration);
    const id = requirementIdValue(declaration.requirement_id);
    if (id <= previous)
      fail("required_declarations must be sorted and unique");
    previous = id;
    if (!["one", "optional", "many"].includes(String(declaration.cardinality)))
      fail("unknown requirement cardinality");
    byId.set(id, declaration);
  }
  const counts = new Map;
  const orders = new Map;
  const routeIds = new Set;
  const providers = new Set;
  let previousRoute;
  for (const candidate of routes) {
    const route = object(candidate, "route");
    exact(route, [
      "route_id",
      "requirement_id",
      "capability_id",
      "descriptor_version",
      "descriptor_digest",
      "provider_instance",
      "provider_order"
    ]);
    descriptor(route);
    const routeId = token2(route.route_id, "route_id");
    if (routeIds.has(routeId))
      fail("route_id must be unique within a session");
    routeIds.add(routeId);
    const requirementId = requirementIdValue(route.requirement_id);
    const provider = token2(route.provider_instance, "provider_instance");
    const declaration = byId.get(requirementId);
    if (!declaration)
      fail("route references an unknown requirement_id");
    if (route.capability_id !== declaration.capability_id || route.descriptor_version !== declaration.descriptor_version || route.descriptor_digest !== declaration.descriptor_digest)
      fail("route descriptor does not match its named requirement");
    const expected = orders.get(requirementId) ?? 0;
    const providerOrder = integer(route.provider_order, "provider_order", 0, 1048576);
    const sortKey = [requirementId, providerOrder, provider];
    if (previousRoute && compareRoute(previousRoute, sortKey) >= 0)
      fail("routes must be sorted by requirement_id, provider_order and provider_instance");
    previousRoute = sortKey;
    if (providerOrder !== expected)
      fail("requirement routes must use contiguous provider_order values");
    const providerKey = `${requirementId}\x00${provider}`;
    if (providers.has(providerKey))
      fail("one requirement cannot bind the same provider twice");
    providers.add(providerKey);
    orders.set(requirementId, expected + 1);
    counts.set(requirementId, (counts.get(requirementId) ?? 0) + 1);
  }
  for (const [id, declaration] of byId) {
    const count = counts.get(id) ?? 0;
    if (declaration.cardinality === "one" && count !== 1)
      fail("one requirement must bind exactly one route");
    if (declaration.cardinality === "optional" && count > 1)
      fail("optional requirement may bind at most one route");
  }
  previous = "";
  for (const candidate of endpoints) {
    const endpoint = object(candidate, "provided endpoint");
    exact(endpoint, [
      "endpoint_id",
      "capability_id",
      "descriptor_version",
      "descriptor_digest"
    ]);
    descriptor(endpoint);
    const id = token2(endpoint.endpoint_id, "endpoint_id");
    if (id <= previous)
      fail("provided_endpoints must be sorted and unique");
    previous = id;
  }
}
function validateInitializeForRuntimeProfile(value, expected) {
  validateInitialize(value);
  if (value.identity.runtime_profile !== expected)
    fail("runtime profile does not match the selected Adapter profile");
}
function validateInitialized(value, expected) {
  validateInitialize(value);
  if (canonicalJson(value) !== canonicalJson(expected))
    fail("initialized result does not exactly echo initialization");
}
function validateInvocationScope(value) {
  const scope = object(value, "invocation scope");
  exact(scope, [
    "scope_id",
    "parent_scope_id",
    "remaining_budget_nanos",
    "permissions",
    "extensions"
  ]);
  token2(scope.scope_id, "scope_id");
  if (scope.parent_scope_id !== null)
    token2(scope.parent_scope_id, "parent_scope_id");
  decimal2(scope.remaining_budget_nanos, "remaining_budget_nanos");
  sortedTokens(scope.permissions, "permissions");
  const extensions = array(scope.extensions, "extensions");
  let previous = "";
  for (const candidate of extensions) {
    const extension = object(candidate, "extension");
    validateExtension(extension);
    const key = token2(extension.key, "extension key");
    if (key <= previous)
      fail("extensions must be sorted and unique");
    previous = key;
  }
}
function validateInvoke(value, initialize) {
  validateAuthoringMessage(value, "invoke", initialize.identity);
  const invoke = value;
  const endpoint = initialize.provided_endpoints.find((candidate) => candidate.endpoint_id === invoke.endpoint_id);
  if (!endpoint)
    fail("invoke references an unknown endpoint_id");
  if (endpoint.capability_id !== invoke.capability_id || endpoint.descriptor_version !== invoke.descriptor_version || endpoint.descriptor_digest !== invoke.descriptor_digest)
    fail("invoke descriptor does not match its admitted endpoint");
}
function validateOutboundCall(value, initialize, parent, parentActive) {
  validateAuthoringMessage(value, "outbound_call", initialize.identity, parent);
  if (!parentActive)
    fail("closed parent scope cannot start an outbound call");
  const call = value;
  const route = initialize.routes.find((candidate) => candidate.route_id === call.route_id);
  if (!route)
    fail("outbound call references an unknown route_id");
  if (route.requirement_id !== call.requirement_id)
    fail("outbound route belongs to another requirement");
}
function validateEventPublish(value, initialize) {
  validateAuthoringMessage(value, "event_publish", initialize.identity);
  validateProvidedOperation(value, initialize, "event publication");
}
function validateOutboundEventPublish(value, initialize, parent, parentActive) {
  validateAuthoringMessage(value, "outbound_event_publish", initialize.identity, parent);
  validateOutboundOperation(value, initialize, parentActive, "event publication");
}
function validateEventPublishResultFor(value, identity, correlationId) {
  validateAuthoringMessage(value, "event_publish_result", identity);
  if (value.correlation_id !== correlationId)
    fail("response identity mismatch");
}
function validateStreamOpen(value, initialize) {
  validateAuthoringMessage(value, "stream_open", initialize.identity);
  validateProvidedOperation(value, initialize, "stream open");
}
function validateOutboundStreamOpen(value, initialize, parent, parentActive) {
  validateAuthoringMessage(value, "outbound_stream_open", initialize.identity, parent);
  validateOutboundOperation(value, initialize, parentActive, "stream open");
}
function validateStreamOpenResultFor(value, identity, correlationId) {
  validateAuthoringMessage(value, "stream_open_result", identity);
  if (value.correlation_id !== correlationId)
    fail("response identity mismatch");
}
function validateStreamAction(value, identity, kind) {
  validateAuthoringMessage(value, kind, identity);
}
function validateStreamResultFor(value, identity, correlationId, streamId, kind) {
  validateAuthoringMessage(value, kind, identity);
  const result = value;
  if (result.correlation_id !== correlationId)
    fail("response identity mismatch");
  if (result.stream_id !== streamId)
    fail("stream result identity mismatch");
}
function validateResultFor(value, identity, correlationId) {
  validateAuthoringMessage(value, "result", identity);
  if (value.correlation_id !== correlationId)
    fail("response identity mismatch");
}
function validateChildScope(value, parent) {
  validateInvocationScope(value);
  validateInvocationScope(parent);
  if (value.parent_scope_id !== parent.scope_id)
    fail("outbound scope must name its exact parent scope");
  if (BigInt(value.remaining_budget_nanos) > BigInt(parent.remaining_budget_nanos))
    fail("outbound remaining budget may not increase");
  if (canonicalJson(value.permissions) !== canonicalJson(parent.permissions) || canonicalJson(value.extensions) !== canonicalJson(parent.extensions))
    fail("outbound scope must preserve permissions and extensions");
}
function validateAuthoringMessage(value, kind, identity, parent) {
  validateIdentity(identity);
  const message = object(value, kind);
  const session = token2(message.session, "session");
  if (session !== identity.session)
    fail("message session does not match admitted session");
  switch (kind) {
    case "construct":
      exact(message, [
        "session",
        "lifecycle_scope_id",
        "remaining_budget_nanos"
      ]);
      token2(message.lifecycle_scope_id, "lifecycle_scope_id");
      decimal2(message.remaining_budget_nanos, "remaining_budget_nanos");
      return;
    case "constructed":
      exact(message, ["session", "lifecycle_scope_id", "outcome"]);
      token2(message.lifecycle_scope_id, "lifecycle_scope_id");
      validateOutcome(message.outcome, true);
      return;
    case "invoke":
      exact(message, [
        "session",
        "correlation_id",
        "endpoint_id",
        "capability_id",
        "descriptor_version",
        "descriptor_digest",
        "operation",
        "scope",
        "payload"
      ]);
      decimal2(message.correlation_id, "correlation_id");
      token2(message.endpoint_id, "endpoint_id");
      descriptor(message);
      token2(message.operation, "operation");
      validateInvocationScope(message.scope);
      return;
    case "result":
      exact(message, ["session", "correlation_id", "outcome"]);
      decimal2(message.correlation_id, "correlation_id");
      validateOutcome(message.outcome, false);
      return;
    case "outbound_call":
      exact(message, [
        "session",
        "correlation_id",
        "requirement_id",
        "route_id",
        "operation",
        "scope",
        "payload"
      ]);
      decimal2(message.correlation_id, "correlation_id");
      requirementIdValue(message.requirement_id);
      token2(message.route_id, "route_id");
      token2(message.operation, "operation");
      if (!parent)
        fail("outbound call requires its parent scope");
      validateChildScope(message.scope, parent);
      return;
    case "event_publish":
      validateProvidedWireOperation(message, "event");
      return;
    case "outbound_event_publish":
      validateOutboundWireOperation(message, "event", parent);
      return;
    case "event_publish_result": {
      exact(message, ["session", "correlation_id", "outcome"]);
      decimal2(message.correlation_id, "correlation_id");
      const outcome = object(message.outcome, "event publish outcome");
      if (outcome.kind === "accepted")
        exact(outcome, ["kind"]);
      else if (outcome.kind === "runtime") {
        exact(outcome, ["kind", "failure"]);
        validateRuntimeFailure(outcome.failure);
      } else
        fail("unknown event publish outcome");
      return;
    }
    case "stream_open":
      validateProvidedWireOperation(message, "request");
      return;
    case "outbound_stream_open":
      validateOutboundWireOperation(message, "request", parent);
      return;
    case "stream_open_result": {
      exact(message, ["session", "correlation_id", "outcome"]);
      decimal2(message.correlation_id, "correlation_id");
      const outcome = object(message.outcome, "stream open outcome");
      if (outcome.kind === "opened") {
        exact(outcome, ["kind", "stream_id"]);
        decimal2(outcome.stream_id, "stream_id");
      } else if (outcome.kind === "domain")
        exact(outcome, ["kind", "error"]);
      else if (outcome.kind === "runtime") {
        exact(outcome, ["kind", "failure"]);
        validateRuntimeFailure(outcome.failure);
      } else
        fail("unknown stream open outcome");
      return;
    }
    case "stream_send":
      exact(message, ["session", "correlation_id", "stream_id", "sequence", "message"]);
      validateStreamWireIdentity(message);
      decimal2(message.sequence, "sequence");
      return;
    case "stream_receive":
    case "stream_close_send":
    case "stream_cancel":
      exact(message, ["session", "correlation_id", "stream_id"]);
      validateStreamWireIdentity(message);
      return;
    case "stream_action_result": {
      exact(message, ["session", "correlation_id", "stream_id", "outcome"]);
      validateStreamWireIdentity(message);
      const outcome = object(message.outcome, "stream action outcome");
      if (outcome.kind === "accepted")
        exact(outcome, ["kind"]);
      else if (outcome.kind === "runtime") {
        exact(outcome, ["kind", "failure"]);
        validateRuntimeFailure(outcome.failure);
      } else
        fail("unknown stream action outcome");
      return;
    }
    case "stream_receive_result": {
      exact(message, ["session", "correlation_id", "stream_id", "outcome"]);
      validateStreamWireIdentity(message);
      validateStreamReceiveOutcome(message.outcome);
      return;
    }
    case "cancel":
      exact(message, ["session", "scope_id", "correlation_id", "reason"]);
      token2(message.scope_id, "scope_id");
      decimal2(message.correlation_id, "correlation_id");
      detail(message.reason);
      return;
    case "cancel_ack":
      exact(message, ["session", "scope_id", "correlation_id", "accepted"]);
      token2(message.scope_id, "scope_id");
      decimal2(message.correlation_id, "correlation_id");
      if (message.accepted !== true)
        fail("cancel acknowledgement must be accepted");
      return;
    case "settlement":
      exact(message, ["session", "scope_id", "correlation_id", "state"]);
      token2(message.scope_id, "scope_id");
      decimal2(message.correlation_id, "correlation_id");
      if (!["completed", "cancelled", "abandoned"].includes(String(message.state)))
        fail("unknown settlement state");
      return;
    case "stop":
      exact(message, ["session", "cleanup_scope_id", "remaining_budget_nanos"]);
      token2(message.cleanup_scope_id, "cleanup_scope_id");
      decimal2(message.remaining_budget_nanos, "remaining_budget_nanos");
      return;
    case "stopped":
      exact(message, ["session", "cleanup_scope_id", "hook", "diagnostics"]);
      token2(message.cleanup_scope_id, "cleanup_scope_id");
      if (!["not_declared", "completed", "failed"].includes(String(message.hook)))
        fail("unknown stop hook outcome");
      for (const candidate of array(message.diagnostics, "diagnostics")) {
        const diagnostic = object(candidate, "diagnostic");
        exact(diagnostic, ["code", "detail"]);
        token2(diagnostic.code, "diagnostic code");
        detail(diagnostic.detail);
      }
      return;
  }
}
function parseAuthoringFrame(wire, maxBytes = DEFAULT_MAX_FRAME_BYTES) {
  if (new TextEncoder().encode(wire).byteLength > maxBytes)
    fail("Authoring frame exceeds max_frame_bytes");
  return parseStrictJson(wire);
}
function validateProvidedOperation(value, initialize, interaction) {
  const endpoint = initialize.provided_endpoints.find((candidate) => candidate.endpoint_id === value.endpoint_id);
  if (!endpoint)
    fail(`${interaction} references an unknown endpoint_id`);
  if (endpoint.capability_id !== value.capability_id || endpoint.descriptor_version !== value.descriptor_version || endpoint.descriptor_digest !== value.descriptor_digest)
    fail(`${interaction} descriptor does not match its admitted endpoint`);
}
function validateOutboundOperation(value, initialize, parentActive, interaction) {
  if (!parentActive)
    fail(`closed parent scope cannot start an outbound ${interaction}`);
  const route = initialize.routes.find((candidate) => candidate.route_id === value.route_id);
  if (!route)
    fail(`outbound ${interaction} references an unknown route_id`);
  if (route.requirement_id !== value.requirement_id)
    fail("outbound route belongs to another requirement");
}
function validateProvidedWireOperation(message, valueField) {
  exact(message, [
    "session",
    "correlation_id",
    "endpoint_id",
    "capability_id",
    "descriptor_version",
    "descriptor_digest",
    "operation",
    "scope",
    valueField
  ]);
  decimal2(message.correlation_id, "correlation_id");
  token2(message.endpoint_id, "endpoint_id");
  descriptor(message);
  token2(message.operation, "operation");
  validateInvocationScope(message.scope);
}
function validateOutboundWireOperation(message, valueField, parent) {
  exact(message, [
    "session",
    "correlation_id",
    "requirement_id",
    "route_id",
    "operation",
    "scope",
    valueField
  ]);
  decimal2(message.correlation_id, "correlation_id");
  requirementIdValue(message.requirement_id);
  token2(message.route_id, "route_id");
  token2(message.operation, "operation");
  if (!parent)
    fail("outbound operation requires its parent scope");
  validateChildScope(message.scope, parent);
}
function validateStreamWireIdentity(message) {
  decimal2(message.correlation_id, "correlation_id");
  decimal2(message.stream_id, "stream_id");
}
function validateStreamReceiveOutcome(value) {
  const outcome = object(value, "stream receive outcome");
  if (outcome.kind === "message") {
    exact(outcome, ["kind", "sequence", "message"]);
    decimal2(outcome.sequence, "sequence");
    return;
  }
  if (outcome.kind === "peer_half_closed") {
    exact(outcome, ["kind"]);
    return;
  }
  if (outcome.kind === "terminal") {
    exact(outcome, ["kind", "outcome"]);
    const terminal = object(outcome.outcome, "stream terminal outcome");
    if (terminal.kind === "success")
      exact(terminal, ["kind"]);
    else if (terminal.kind === "domain")
      exact(terminal, ["kind", "error"]);
    else
      fail("unknown stream terminal outcome");
    return;
  }
  if (outcome.kind === "runtime") {
    exact(outcome, ["kind", "failure"]);
    validateRuntimeFailure(outcome.failure);
    return;
  }
  fail("unknown stream receive outcome");
}
function validateIdentity(value) {
  const identity = object(value, "session identity");
  exact(identity, [
    "session",
    "plugin_instance",
    "plugin_generation",
    "artifact_digest",
    "contract_digest",
    "runtime_profile",
    "value_profile"
  ]);
  token2(identity.session, "session");
  token2(identity.plugin_instance, "plugin_instance");
  decimal2(identity.plugin_generation, "plugin_generation");
  digest2(identity.artifact_digest, "artifact_digest");
  digest2(identity.contract_digest, "contract_digest");
  token2(identity.runtime_profile, "runtime_profile");
  if (identity.value_profile !== VALUE_PROFILE)
    fail("unsupported portable value profile");
}
function validateLimits(value) {
  const limits = object(value, "limits");
  exact(limits, [
    "max_frame_bytes",
    "max_active_invocations",
    "max_active_outbound_calls",
    "max_queued_calls",
    "max_unfinished_executions",
    "max_retired_ids"
  ]);
  integer(limits.max_frame_bytes, "max_frame_bytes", 1, DEFAULT_MAX_FRAME_BYTES);
  integer(limits.max_active_invocations, "max_active_invocations", 1, 1024);
  integer(limits.max_active_outbound_calls, "max_active_outbound_calls", 1, 1024);
  integer(limits.max_queued_calls, "max_queued_calls", 1, 65536);
  integer(limits.max_unfinished_executions, "max_unfinished_executions", 1, 65536);
  integer(limits.max_retired_ids, "max_retired_ids", 1, 1048576);
}
function validateOutcome(value, factory) {
  const outcome = object(value, "outcome");
  const kind = String(outcome.kind);
  if (factory) {
    if (kind === "constructed") {
      exact(outcome, ["kind"]);
      return;
    }
    if (kind === "failed") {
      exact(outcome, ["kind", "detail"]);
      detail(outcome.detail);
      return;
    }
    fail("unknown factory outcome");
  }
  if (kind === "success") {
    exact(outcome, ["kind", "value"]);
    return;
  }
  if (kind === "domain") {
    exact(outcome, ["kind", "error"]);
    return;
  }
  if (kind === "runtime") {
    exact(outcome, ["kind", "failure"]);
    validateRuntimeFailure(outcome.failure);
    return;
  }
  fail("unknown invocation outcome");
}
function validateRuntimeFailure(value) {
  const failure = object(value, "runtime failure");
  const kind = String(failure.kind);
  switch (kind) {
    case "unavailable":
    case "protocol_violation":
      exact(failure, ["kind", "capability"]);
      token2(failure.capability, "capability");
      return;
    case "unknown_operation":
    case "resource_exhausted":
      exact(failure, ["kind", "capability", "operation"]);
      token2(failure.capability, "capability");
      token2(failure.operation, "operation");
      return;
    case "ambiguous_binding":
      exact(failure, ["kind", "capability", "providers"]);
      token2(failure.capability, "capability");
      integer(failure.providers, "providers", 1, 1048576);
      return;
    case "missing_plugin_factory":
      exact(failure, ["kind", "instance", "package_id"]);
      token2(failure.instance, "instance");
      token2(failure.package_id, "package_id");
      return;
    case "unavailable_execution_class":
      exact(failure, ["kind", "instance_key", "execution_class"]);
      token2(failure.instance_key, "instance_key");
      token2(failure.execution_class, "execution_class");
      return;
    case "invalid_resolved_plan":
    case "internal":
    case "plugin_failure":
      exact(failure, ["kind", "detail"]);
      detail(failure.detail);
      return;
    case "admission_closed":
      exact(failure, ["kind"]);
      return;
    case "deadline_exceeded":
    case "cancelled":
      exact(failure, ["kind", "request_id"]);
      decimal2(failure.request_id, "request_id");
      return;
    case "plugin_restart_exhausted":
      exact(failure, ["kind", "instance", "attempts"]);
      token2(failure.instance, "instance");
      integer(failure.attempts, "attempts", 1, 1048576);
      return;
    default:
      fail("unknown runtime failure kind");
  }
}
function descriptor(value) {
  token2(value.capability_id, "capability_id");
  token2(value.descriptor_version, "descriptor_version");
  digest2(value.descriptor_digest, "descriptor_digest");
}
function requirementIdValue(value) {
  if (typeof value !== "string" || !REQUIREMENT_ID.test(value))
    fail("requirement_id must match ^[a-z][a-z0-9_]{0,63}$");
  return value;
}
function compareRoute(left, right) {
  return left[0].localeCompare(right[0]) || left[1] - right[1] || left[2].localeCompare(right[2]);
}
function canonicalJson(value) {
  if (Array.isArray(value))
    return `[${value.map(canonicalJson).join(",")}]`;
  if (typeof value === "object" && value !== null) {
    const record2 = value;
    return `{${Object.keys(record2).sort().map((key) => `${JSON.stringify(key)}:${canonicalJson(record2[key])}`).join(",")}}`;
  }
  return JSON.stringify(value) ?? "undefined";
}
function object(value, name) {
  if (typeof value !== "object" || value === null || Array.isArray(value))
    fail(`${name} must be an object`);
  return value;
}
function array(value, name) {
  if (!Array.isArray(value))
    fail(`${name} must be an array`);
  return value;
}
function exact(value, keys) {
  const actual = Object.keys(value).sort();
  const expected = [...keys].sort();
  if (actual.length !== expected.length || actual.some((key, index) => key !== expected[index]))
    fail("object contains missing or unknown fields");
}
function token2(value, name) {
  if (typeof value !== "string" || !TOKEN2.test(value))
    fail(`${name} must be a portable token`);
  return value;
}
function decimal2(value, name) {
  if (typeof value !== "string" || !DECIMAL2.test(value))
    fail(`${name} must be a canonical decimal string`);
  try {
    const parsed = BigInt(value);
    if (parsed > 18446744073709551615n)
      fail(`${name} exceeds unsigned 64-bit range`);
  } catch {
    fail(`${name} must be a canonical decimal string`);
  }
  return value;
}
function digest2(value, name) {
  if (typeof value !== "string" || !DIGEST2.test(value))
    fail(`${name} must be a canonical SHA-256 digest`);
}
function integer(value, name, minimum, maximum) {
  if (!Number.isSafeInteger(value) || Number(value) < minimum || Number(value) > maximum)
    fail(`${name} must be within ${minimum}..=${maximum}`);
  return Number(value);
}
function sortedTokens(value, name) {
  const values = array(value, name);
  let previous = "";
  for (const candidate of values) {
    const current = token2(candidate, name);
    if (current <= previous)
      fail(`${name} must be sorted and unique`);
    previous = current;
  }
}
function detail(value) {
  if (typeof value !== "string" || value.length === 0 || new TextEncoder().encode(value).byteLength > 1024)
    fail("detail must contain 1..=1024 bytes");
}
function fail(message) {
  throw new Error(message);
}

// src/proof.ts
var encoder = new TextEncoder;
var HOST_DOMAIN = encoder.encode("lenso-process-host-v1");
var CHILD_DOMAIN = encoder.encode("lenso-process-child-v1");
var AUTHORING_HOST_DOMAIN = encoder.encode("lenso-authoring-host-v2");
var AUTHORING_CHILD_DOMAIN = encoder.encode("lenso-authoring-child-v2");
var AUTHORING_CALLBACK_DOMAIN = encoder.encode("lenso-authoring-callback-v2");
var BASE64URL = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_";
function canonicalizeProofValue(value) {
  return encoder.encode(canonical(value));
}
function handshakeProofPayload(params) {
  return canonicalizeProofValue({
    identity: params.identity,
    host_nonce: params.host_nonce
  });
}
function hostProofMessage(handshakeDigest) {
  exactLength(handshakeDigest, 32, "handshake digest");
  return concatenate(HOST_DOMAIN, Uint8Array.of(0), handshakeDigest);
}
function childProofMessage(handshakeDigest, session) {
  exactLength(handshakeDigest, 32, "handshake digest");
  return concatenate(CHILD_DOMAIN, Uint8Array.of(0), handshakeDigest, decodeBase64Url32(session, "session"));
}
function authoringHandshakeProofPayload(input) {
  validateInitialize(input.initialize);
  if (!isLoopbackHttpOrigin(input.callback_origin)) {
    throw new Error("callback_origin must be an exact loopback HTTP origin");
  }
  decodeBase64Url32(input.host_nonce, "host_nonce");
  return canonicalizeProofValue(input);
}
function authoringHostProofMessage(handshakeDigest) {
  exactLength(handshakeDigest, 32, "handshake digest");
  return concatenate(AUTHORING_HOST_DOMAIN, Uint8Array.of(0), handshakeDigest);
}
function authoringChildProofMessage(handshakeDigest, childNonce) {
  exactLength(handshakeDigest, 32, "handshake digest");
  return concatenate(AUTHORING_CHILD_DOMAIN, Uint8Array.of(0), handshakeDigest, decodeBase64Url32(childNonce, "child_nonce"));
}
function authoringCallbackProofMessage(session, method, params) {
  const sessionBytes = decodeBase64Url32(session, "session");
  return concatenate(AUTHORING_CALLBACK_DOMAIN, Uint8Array.of(0), sessionBytes, Uint8Array.of(0), encoder.encode(method), Uint8Array.of(0), canonicalizeProofValue(params));
}
function decodeBase64Url32(value, name = "value") {
  if (!/^[A-Za-z0-9_-]{43}$/.test(value)) {
    throw new Error(`${name} must encode exactly 32 canonical base64url bytes`);
  }
  const decoded = decodeBase64Url(value);
  exactLength(decoded, 32, name);
  if (encodeBase64Url(decoded) !== value) {
    throw new Error(`${name} must use canonical unpadded base64url`);
  }
  return decoded;
}
function encodeBase64Url(value) {
  let output = "";
  for (let index = 0;index < value.length; index += 3) {
    const first = value[index];
    const second = value[index + 1];
    const third = value[index + 2];
    const bits = first << 16 | (second ?? 0) << 8 | (third ?? 0);
    output += BASE64URL[bits >>> 18 & 63];
    output += BASE64URL[bits >>> 12 & 63];
    if (second !== undefined)
      output += BASE64URL[bits >>> 6 & 63];
    if (third !== undefined)
      output += BASE64URL[bits & 63];
  }
  return output;
}
function decodeBase64Url(value) {
  let buffer = 0;
  let bits = 0;
  const output = [];
  for (const character of value) {
    const digit = BASE64URL.indexOf(character);
    if (digit < 0)
      throw new Error("invalid base64url character");
    buffer = buffer << 6 | digit;
    bits += 6;
    if (bits >= 8) {
      bits -= 8;
      output.push(buffer >>> bits & 255);
      buffer &= (1 << bits) - 1;
    }
  }
  if (bits > 0 && buffer !== 0)
    throw new Error("non-canonical base64url tail bits");
  return Uint8Array.from(output);
}
function canonical(value) {
  if (value === null)
    return "null";
  if (typeof value === "boolean")
    return value ? "true" : "false";
  if (typeof value === "string")
    return JSON.stringify(value);
  if (typeof value === "number") {
    if (!Number.isSafeInteger(value)) {
      throw new Error("proof JSON permits only portable safe integers");
    }
    return String(value);
  }
  if (Array.isArray(value))
    return `[${value.map(canonical).join(",")}]`;
  if (typeof value === "object") {
    const candidate = value;
    const entries = Object.keys(candidate).sort().map((key) => `${JSON.stringify(key)}:${canonical(candidate[key])}`);
    return `{${entries.join(",")}}`;
  }
  throw new Error("proof JSON contains an unsupported value");
}
function concatenate(...values) {
  const output = new Uint8Array(values.reduce((length, value) => length + value.length, 0));
  let offset = 0;
  for (const value of values) {
    output.set(value, offset);
    offset += value.length;
  }
  return output;
}
function exactLength(value, expected, name) {
  if (value.length !== expected)
    throw new Error(`${name} must contain ${expected} bytes`);
}
function isLoopbackHttpOrigin(value) {
  const match = /^http:\/\/(?:127\.0\.0\.1|\[::1\]):([1-9][0-9]{0,4})\/$/.exec(value);
  return match !== null && Number(match[1]) <= 65535;
}
