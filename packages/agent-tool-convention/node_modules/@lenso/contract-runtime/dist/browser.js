// src/index.ts
var runtimeFailureKinds = Object.freeze([
  "unavailable",
  "unknown_operation",
  "ambiguous_binding",
  "protocol_violation",
  "missing_plugin_factory",
  "unavailable_execution_class",
  "invalid_resolved_plan",
  "admission_closed",
  "resource_exhausted",
  "deadline_exceeded",
  "cancelled",
  "internal",
  "plugin_failure",
  "plugin_restart_exhausted"
]);
function isRuntimeFailureKind(value) {
  return runtimeFailureKinds.some((kind) => kind === value);
}
var portableValueProfile = {
  int64: "decimal-string",
  uint64: "decimal-string",
  bytes: "base64-string",
  timestamp: "RFC3339-string",
  duration: "ISO8601-string",
  missingAndNull: "distinct"
};
function encodePortableJson(value, subject) {
  validatePortableJson(value);
  const wire = JSON.stringify(value);
  if (wire === undefined)
    throw new Error(`${subject} cannot be encoded`);
  return wire;
}
function decodePortableJson(wire) {
  const value = JSON.parse(wire);
  validatePortableJson(value);
  return value;
}
function decodeDomainError(wire, knownStringCodes) {
  const value = decodePortableJson(wire);
  if (typeof value === "string") {
    if (knownStringCodes.includes(value))
      return value;
    return { code: value };
  }
  if (isRecord(value) && typeof value.code === "string")
    return value;
  throw new Error("Domain Error must be a string or object");
}
function validatePortableJson(value) {
  if (typeof value === "number") {
    if (!Number.isFinite(value) || Number.isInteger(value) && !Number.isSafeInteger(value)) {
      throw new Error("wire JSON contains an unsafe number");
    }
    return;
  }
  if (Array.isArray(value)) {
    for (const item of value)
      validatePortableJson(item);
    return;
  }
  if (isRecord(value)) {
    for (const item of Object.values(value))
      validatePortableJson(item);
  }
}
function isRecord(value) {
  return typeof value === "object" && value !== null;
}

// src/browser.ts
function validateSchema(value, schema, label) {
  validatePortableJson(value);
  if (!isSupportedSchema(schema) || !matchesSchema(value, schema)) {
    throw new Error(`invalid ${label} payload`);
  }
}
function validateResult(result, responseSchema, errorSchema) {
  if (!isRecord2(result) || typeof result.ok !== "boolean") {
    throw new Error("invalid Capability result envelope");
  }
  if (result.ok) {
    if (!Object.hasOwn(result, "value")) {
      throw new Error("successful Capability result is missing value");
    }
    validateSchema(result.value, responseSchema, "response");
    return;
  }
  if (!isRecord2(result.error) || !["domain", "runtime"].includes(String(result.error.kind))) {
    throw new Error("invalid invocation error envelope");
  }
  if (result.error.kind === "domain") {
    validateDomainError(result.error.error, errorSchema);
  } else if (!isRecord2(result.error.error) || !isRuntimeFailureKind(result.error.error.kind)) {
    throw new Error("invalid Runtime Failure payload");
  }
}
function validateDomainError(value, schema) {
  if (!isSupportedSchema(schema)) {
    throw new Error("invalid Domain Error payload");
  }
  if (matchesSchema(value, schema))
    return;
  if (typeof value === "string")
    return;
  if (isRecord2(value) && typeof value.code === "string")
    return;
  throw new Error("invalid Domain Error payload");
}
function matchesSchema(value, schema) {
  const oneOf = schemaList(schema.oneOf);
  if (oneOf && oneOf.filter((candidate) => matchesSchema(value, candidate)).length !== 1) {
    return false;
  }
  const anyOf = schemaList(schema.anyOf);
  if (anyOf && !anyOf.some((candidate) => matchesSchema(value, candidate))) {
    return false;
  }
  if (Object.hasOwn(schema, "const") && !jsonDeepEqual(value, schema.const)) {
    return false;
  }
  if (Array.isArray(schema.enum) && !schema.enum.some((candidate) => jsonDeepEqual(value, candidate))) {
    return false;
  }
  if (Array.isArray(schema.type)) {
    return schema.type.some((type) => matchesSchema(value, { ...schema, type }));
  }
  if (schema.type === "null")
    return value === null;
  if (schema.type === "string") {
    if (typeof value !== "string" || !matchesFormat(value, schema.format)) {
      return false;
    }
    if (typeof schema.pattern === "string" && !matchesUnicodePattern(value, schema.pattern)) {
      return false;
    }
    const length = Array.from(value).length;
    if (typeof schema.minLength === "number" && length < schema.minLength) {
      return false;
    }
    if (typeof schema.maxLength === "number" && length > schema.maxLength) {
      return false;
    }
  }
  if (schema.type === "boolean" && typeof value !== "boolean")
    return false;
  if (schema.type === "number" && (typeof value !== "number" || !Number.isFinite(value))) {
    return false;
  }
  if (schema.type === "integer" && !Number.isSafeInteger(value))
    return false;
  if (schema.type === "number" || schema.type === "integer") {
    if (typeof value !== "number" || !matchesNumericBounds(value, schema)) {
      return false;
    }
  }
  if (schema.type === "array") {
    if (!Array.isArray(value))
      return false;
    if (typeof schema.minItems === "number" && value.length < schema.minItems) {
      return false;
    }
    if (typeof schema.maxItems === "number" && value.length > schema.maxItems) {
      return false;
    }
    if (schema.uniqueItems === true && containsDeepDuplicate(value)) {
      return false;
    }
    if (isRecord2(schema.items)) {
      return value.every((item) => matchesSchema(item, schema.items));
    }
  }
  if (schema.type === "object") {
    if (!isRecord2(value) || Array.isArray(value))
      return false;
    const required = Array.isArray(schema.required) ? schema.required : [];
    if (required.some((key) => typeof key !== "string" || !Object.hasOwn(value, key))) {
      return false;
    }
    const properties = isRecord2(schema.properties) ? schema.properties : {};
    for (const [key, item] of Object.entries(value)) {
      const property = properties[key];
      if (isRecord2(property)) {
        if (!matchesSchema(item, property))
          return false;
      } else if (schema.additionalProperties === false) {
        return false;
      } else if (isRecord2(schema.additionalProperties) && !matchesSchema(item, schema.additionalProperties)) {
        return false;
      }
    }
  }
  return true;
}
var supportedSchemaKeywords = new Set([
  "$defs",
  "additionalProperties",
  "anyOf",
  "const",
  "enum",
  "exclusiveMaximum",
  "exclusiveMinimum",
  "format",
  "items",
  "maxItems",
  "maxLength",
  "maximum",
  "minItems",
  "minLength",
  "minimum",
  "oneOf",
  "pattern",
  "properties",
  "required",
  "type",
  "uniqueItems"
]);
var harmlessAnnotationKeywords = new Set([
  "$anchor",
  "$comment",
  "$dynamicAnchor",
  "$id",
  "$schema",
  "default",
  "deprecated",
  "description",
  "examples",
  "readOnly",
  "title",
  "writeOnly",
  "x-lenso-sensitive"
]);
var portableSchemaTypes = new Set([
  "array",
  "boolean",
  "integer",
  "null",
  "number",
  "object",
  "string"
]);
var portableFormats = new Set([
  "byte",
  "date-time",
  "duration",
  "int64",
  "uint64"
]);
function isSupportedSchema(schema) {
  if (Object.keys(schema).some((keyword) => !supportedSchemaKeywords.has(keyword) && !harmlessAnnotationKeywords.has(keyword))) {
    return false;
  }
  if (Object.hasOwn(schema, "type")) {
    if (typeof schema.type === "string") {
      if (!portableSchemaTypes.has(schema.type))
        return false;
    } else if (!Array.isArray(schema.type) || schema.type.length === 0 || schema.type.some((type, index, types) => typeof type !== "string" || !portableSchemaTypes.has(type) || types.indexOf(type) !== index)) {
      return false;
    }
  }
  for (const keyword of ["oneOf", "anyOf"]) {
    if (Object.hasOwn(schema, keyword) && (!Array.isArray(schema[keyword]) || schema[keyword].length === 0 || schema[keyword].some((candidate) => !isSchemaObject(candidate) || !isSupportedSchema(candidate)))) {
      return false;
    }
  }
  if (Object.hasOwn(schema, "enum") && (!Array.isArray(schema.enum) || schema.enum.length === 0)) {
    return false;
  }
  if (Object.hasOwn(schema, "required") && (!Array.isArray(schema.required) || schema.required.some((key) => typeof key !== "string"))) {
    return false;
  }
  if (Object.hasOwn(schema, "pattern") && (typeof schema.pattern !== "string" || !isValidUnicodePattern(schema.pattern))) {
    return false;
  }
  if (Object.hasOwn(schema, "format") && (typeof schema.format !== "string" || !portableFormats.has(schema.format))) {
    return false;
  }
  if (Object.hasOwn(schema, "uniqueItems") && typeof schema.uniqueItems !== "boolean") {
    return false;
  }
  for (const keyword of ["minLength", "maxLength", "minItems", "maxItems"]) {
    if (Object.hasOwn(schema, keyword) && (!Number.isSafeInteger(schema[keyword]) || Number(schema[keyword]) < 0)) {
      return false;
    }
  }
  for (const keyword of [
    "minimum",
    "maximum",
    "exclusiveMinimum",
    "exclusiveMaximum"
  ]) {
    if (Object.hasOwn(schema, keyword) && (typeof schema[keyword] !== "number" || !Number.isFinite(schema[keyword]))) {
      return false;
    }
  }
  if (Object.hasOwn(schema, "items")) {
    if (!isSchemaObject(schema.items) || !isSupportedSchema(schema.items)) {
      return false;
    }
  }
  if (Object.hasOwn(schema, "properties")) {
    if (!isSchemaObject(schema.properties) || Object.values(schema.properties).some((property) => !isSchemaObject(property) || !isSupportedSchema(property))) {
      return false;
    }
  }
  if (Object.hasOwn(schema, "$defs")) {
    if (!isSchemaObject(schema.$defs) || Object.values(schema.$defs).some((definition) => !isSchemaObject(definition) || !isSupportedSchema(definition))) {
      return false;
    }
  }
  if (Object.hasOwn(schema, "additionalProperties") && typeof schema.additionalProperties !== "boolean" && (!isSchemaObject(schema.additionalProperties) || !isSupportedSchema(schema.additionalProperties))) {
    return false;
  }
  return true;
}
var MAX_PORTABLE_PATTERN_CODE_POINTS = 4096;
var MAX_PORTABLE_REPETITION = 1e4;
var PORTABLE_UNICODE_PROPERTY_ESCAPE = "\\p{Letter}";
function hasPortablePatternSyntax(pattern) {
  if (Array.from(pattern).length > MAX_PORTABLE_PATTERN_CODE_POINTS || pattern[0] !== "^") {
    return false;
  }
  let index = 0;
  let inClass = false;
  let classStart;
  const frames = [newPortablePatternFrame()];
  while (index < pattern.length) {
    const character = pattern[index];
    if (character === undefined)
      return false;
    if (inClass) {
      if (character === "\\") {
        if (pattern.startsWith(PORTABLE_UNICODE_PROPERTY_ESCAPE, index)) {
          index += PORTABLE_UNICODE_PROPERTY_ESCAPE.length;
          continue;
        }
        const escaped = pattern[index + 1];
        const sharedClassEscape = escaped !== undefined && "^$\\.*+?()[]{}|/-".includes(escaped);
        if (!sharedClassEscape)
          return false;
        index += 2;
        continue;
      }
      if (character === "]") {
        if (classStart === undefined)
          return false;
        const content = pattern.slice(classStart, index);
        if (content === "" || content === "^")
          return false;
        inClass = false;
        classStart = undefined;
        const frame3 = frames[frames.length - 1];
        if (frame3 === undefined || !pushPortablePatternAtom(frame3, newPortablePatternAtom())) {
          return false;
        }
        index += 1;
        continue;
      }
      if (character === "[")
        return false;
      if (["&", "-", "~", "|"].includes(character) && pattern[index + 1] === character) {
        return false;
      }
      const codePoint2 = pattern.codePointAt(index);
      if (!isPortablePatternCodePoint(codePoint2))
        return false;
      index += codePoint2 > 65535 ? 2 : 1;
      continue;
    }
    if (character === "\\") {
      if (pattern.startsWith(PORTABLE_UNICODE_PROPERTY_ESCAPE, index)) {
        index += PORTABLE_UNICODE_PROPERTY_ESCAPE.length;
      } else {
        const escaped = pattern[index + 1];
        const sharedSyntaxEscape = escaped !== undefined && "^$\\.*+?()[]{}|/".includes(escaped);
        if (!sharedSyntaxEscape)
          return false;
        index += 2;
      }
      const frame3 = frames[frames.length - 1];
      if (frame3 === undefined || !pushPortablePatternAtom(frame3, newPortablePatternAtom())) {
        return false;
      }
      continue;
    }
    if (character === "[") {
      inClass = true;
      classStart = index + 1;
      index += 1;
      continue;
    }
    if (character === "]" || character === "}")
      return false;
    if (character === ".")
      return false;
    if (character === "(") {
      if (pattern[index + 1] === "?") {
        if (!pattern.startsWith("(?:", index))
          return false;
        index += 3;
      } else {
        index += 1;
      }
      frames.push(newPortablePatternFrame());
      continue;
    }
    if (character === ")") {
      if (frames.length === 1)
        return false;
      const group = frames.pop();
      if (group === undefined)
        return false;
      const canMatchEmpty = group.earlierBranchCanMatchEmpty || group.sequenceCanMatchEmpty;
      if (canMatchEmpty)
        return false;
      const atom = newPortablePatternAtom({
        canMatchEmpty,
        hasVariableShape: group.containsVariableRepetition || group.containsAlternation,
        containsVariableRepetition: group.containsVariableRepetition,
        containsAlternation: group.containsAlternation,
        isGroup: true
      });
      const parent = frames[frames.length - 1];
      if (parent === undefined || !pushPortablePatternAtom(parent, atom))
        return false;
      index += 1;
      continue;
    }
    if (character === "|") {
      if (frames.length === 1)
        return false;
      const frame3 = frames[frames.length - 1];
      if (frame3 === undefined)
        return false;
      frame3.containsAlternation = true;
      frame3.earlierBranchCanMatchEmpty ||= frame3.sequenceCanMatchEmpty;
      frame3.sequenceCanMatchEmpty = true;
      frame3.branchHasVariableShape = false;
      frame3.lastAtom = undefined;
      index += 1;
      continue;
    }
    if (character === "*" || character === "+" || character === "?") {
      const repetition = character === "*" ? { minimum: 0, maximum: undefined } : character === "+" ? { minimum: 1, maximum: undefined } : { minimum: 0, maximum: 1 };
      const frame3 = frames[frames.length - 1];
      if (frame3 === undefined || !applyPortableRepetition(frame3, repetition)) {
        return false;
      }
      index += 1;
      continue;
    }
    if (character === "{") {
      const close = pattern.indexOf("}", index + 1);
      if (close < 0)
        return false;
      const repetition = parsePortableRepetition(pattern.slice(index + 1, close));
      const frame3 = frames[frames.length - 1];
      if (repetition === undefined || frame3 === undefined || !applyPortableRepetition(frame3, repetition)) {
        return false;
      }
      index = close + 1;
      continue;
    }
    if (character === "^" && index === 0) {
      const frame3 = frames[frames.length - 1];
      if (frame3 === undefined)
        return false;
      frame3.lastAtom = undefined;
      index += 1;
      continue;
    }
    if (character === "$" && isPortableEndAnchor(pattern, index)) {
      const frame3 = frames[frames.length - 1];
      if (frame3 === undefined)
        return false;
      frame3.lastAtom = undefined;
      index += 1;
      continue;
    }
    if (character === "^" || character === "$")
      return false;
    const codePoint = pattern.codePointAt(index);
    if (!isPortablePatternCodePoint(codePoint))
      return false;
    const frame2 = frames[frames.length - 1];
    if (frame2 === undefined || !pushPortablePatternAtom(frame2, newPortablePatternAtom())) {
      return false;
    }
    index += codePoint > 65535 ? 2 : 1;
  }
  if (inClass || frames.length !== 1)
    return false;
  const frame = frames[0];
  if (frame === undefined)
    return false;
  return hasPortableEndAnchor(pattern) || frame.lastAtom !== undefined && !frame.containsVariableRepetition && !frame.containsAlternation;
}
function hasPortableEndAnchor(pattern) {
  if (pattern[pattern.length - 1] !== "$")
    return false;
  let precedingBackslashes = 0;
  for (let index = pattern.length - 2;index >= 0 && pattern[index] === "\\"; index -= 1) {
    precedingBackslashes += 1;
  }
  return precedingBackslashes % 2 === 0;
}
function isPortableEndAnchor(pattern, index) {
  return index === pattern.length - 1 && hasPortableEndAnchor(pattern);
}
function newPortablePatternAtom(overrides = {}) {
  return {
    canMatchEmpty: false,
    hasVariableShape: false,
    isQuantified: false,
    prefixCanMatchEmpty: false,
    containsVariableRepetition: false,
    containsAlternation: false,
    isGroup: false,
    ...overrides
  };
}
function newPortablePatternFrame() {
  return {
    sequenceCanMatchEmpty: true,
    earlierBranchCanMatchEmpty: false,
    containsVariableRepetition: false,
    containsAlternation: false,
    branchHasVariableShape: false,
    lastAtom: undefined
  };
}
function pushPortablePatternAtom(frame, atom) {
  if (atom.hasVariableShape && frame.branchHasVariableShape)
    return false;
  atom.prefixCanMatchEmpty = frame.sequenceCanMatchEmpty;
  frame.sequenceCanMatchEmpty &&= atom.canMatchEmpty;
  frame.branchHasVariableShape ||= atom.hasVariableShape;
  frame.containsVariableRepetition ||= atom.containsVariableRepetition;
  frame.containsAlternation ||= atom.containsAlternation;
  frame.lastAtom = atom;
  return true;
}
function applyPortableRepetition(frame, repetition) {
  const atom = frame.lastAtom;
  if (atom === undefined || atom.isQuantified || repetition.maximum === 0) {
    return false;
  }
  if (atom.isGroup && (atom.canMatchEmpty || atom.containsVariableRepetition || atom.containsAlternation)) {
    return false;
  }
  const hasVariableExtent = repetition.maximum !== repetition.minimum;
  if (hasVariableExtent && frame.branchHasVariableShape)
    return false;
  atom.isQuantified = true;
  atom.hasVariableShape ||= hasVariableExtent;
  atom.canMatchEmpty = repetition.minimum === 0 || atom.canMatchEmpty;
  frame.sequenceCanMatchEmpty = atom.prefixCanMatchEmpty && atom.canMatchEmpty;
  frame.branchHasVariableShape ||= hasVariableExtent;
  frame.containsVariableRepetition ||= hasVariableExtent;
  return true;
}
function parsePortableRepetition(value) {
  const parts = value.split(",");
  if (parts.length > 2)
    return;
  const minimumText = parts[0];
  if (minimumText === undefined)
    return;
  const minimum = parsePortableRepetitionBound(minimumText);
  if (minimum === undefined)
    return;
  const maximumText = parts[1];
  if (maximumText === undefined)
    return { minimum, maximum: minimum };
  if (maximumText === "")
    return { minimum, maximum: undefined };
  const maximum = parsePortableRepetitionBound(maximumText);
  return maximum !== undefined && maximum >= minimum ? { minimum, maximum } : undefined;
}
function parsePortableRepetitionBound(value) {
  if (!/^(0|[1-9][0-9]*)$/.test(value))
    return;
  const bound = Number(value);
  return bound <= MAX_PORTABLE_REPETITION ? bound : undefined;
}
function isPortablePatternCodePoint(codePoint) {
  return !(codePoint === undefined || codePoint < 32 || codePoint >= 127 && codePoint <= 159 || codePoint >= 55296 && codePoint <= 57343);
}
function isValidUnicodePattern(pattern) {
  if (!hasPortablePatternSyntax(pattern))
    return false;
  try {
    new RegExp(normalizePortableEcmaAnchors(pattern), "u");
    return true;
  } catch {
    return false;
  }
}
function matchesUnicodePattern(value, pattern) {
  try {
    return new RegExp(normalizePortableEcmaAnchors(pattern), "u").test(value);
  } catch {
    return false;
  }
}
function normalizePortableEcmaAnchors(pattern) {
  let normalized = "";
  let index = 0;
  let inClass = false;
  while (index < pattern.length) {
    const character = pattern[index];
    if (character === undefined)
      return pattern;
    if (character === "\\") {
      normalized += pattern.slice(index, index + 2);
      index += 2;
      continue;
    }
    if (character === "[")
      inClass = true;
    if (character === "]")
      inClass = false;
    if (character === "$" && !inClass) {
      normalized += "$(?![\\s\\S])";
      index += 1;
      continue;
    }
    const codePoint = pattern.codePointAt(index);
    if (codePoint === undefined)
      return pattern;
    const width = codePoint > 65535 ? 2 : 1;
    normalized += pattern.slice(index, index + width);
    index += width;
  }
  return normalized;
}
function containsDeepDuplicate(values) {
  const seen = new Set;
  for (const value of values) {
    const fingerprint = jsonSemanticFingerprint(value);
    if (seen.has(fingerprint))
      return true;
    seen.add(fingerprint);
  }
  return false;
}
function jsonSemanticFingerprint(value) {
  if (value === null)
    return "null";
  if (typeof value === "boolean")
    return `boolean:${String(value)}`;
  if (typeof value === "number") {
    return `number:${value === 0 ? "0" : JSON.stringify(value)}`;
  }
  if (typeof value === "string")
    return `string:${JSON.stringify(value)}`;
  if (Array.isArray(value)) {
    return `array:${JSON.stringify(value.map(jsonSemanticFingerprint))}`;
  }
  if (isSchemaObject(value)) {
    const entries = Object.keys(value).sort().map((key) => [key, jsonSemanticFingerprint(value[key])]);
    return `object:${JSON.stringify(entries)}`;
  }
  return `non-json:${typeof value}:${String(value)}`;
}
function jsonDeepEqual(left, right) {
  if (left === right)
    return true;
  if (Array.isArray(left) || Array.isArray(right)) {
    return Array.isArray(left) && Array.isArray(right) && left.length === right.length && left.every((value, index) => jsonDeepEqual(value, right[index]));
  }
  if (!isSchemaObject(left) || !isSchemaObject(right))
    return false;
  const leftKeys = Object.keys(left);
  const rightKeys = Object.keys(right);
  return leftKeys.length === rightKeys.length && leftKeys.every((key) => Object.hasOwn(right, key) && jsonDeepEqual(left[key], right[key]));
}
function matchesNumericBounds(value, schema) {
  return !(typeof schema.minimum === "number" && value < schema.minimum || typeof schema.maximum === "number" && value > schema.maximum || typeof schema.exclusiveMinimum === "number" && value <= schema.exclusiveMinimum || typeof schema.exclusiveMaximum === "number" && value >= schema.exclusiveMaximum);
}
function matchesFormat(value, format) {
  if (!format)
    return true;
  if (format === "int64")
    return decimalInRange(value, true);
  if (format === "uint64")
    return decimalInRange(value, false);
  if (format === "byte")
    return isCanonicalBase64(value);
  if (format === "date-time")
    return isRfc3339(value);
  if (format === "duration")
    return isIso8601Duration(value);
  return false;
}
function decimalInRange(value, signed) {
  const pattern = signed ? /^-?(0|[1-9][0-9]*)$/ : /^(0|[1-9][0-9]*)$/;
  if (!pattern.test(value))
    return false;
  try {
    const number = BigInt(value);
    return signed ? number >= -9223372036854775808n && number <= 9223372036854775807n : number >= 0n && number <= 18446744073709551615n;
  } catch {
    return false;
  }
}
function isCanonicalBase64(value) {
  if (value === "")
    return true;
  if (value.length % 4 !== 0 || !/^[A-Za-z0-9+/]*={0,2}$/.test(value)) {
    return false;
  }
  try {
    return btoa(atob(value)) === value;
  } catch {
    return false;
  }
}
function isRfc3339(value) {
  const match = /^(\d{4})-(\d{2})-(\d{2})[Tt](\d{2}):(\d{2}):(\d{2})(?:\.\d+)?(?:[Zz]|([+-])(\d{2}):(\d{2}))$/.exec(value);
  if (!match)
    return false;
  const [, year, month, day, hour, minute, second, , offsetHour = "0", offsetMinute = "0"] = match;
  const numericYear = Number(year);
  const numericMonth = Number(month);
  const leap = numericYear % 4 === 0 && (numericYear % 100 !== 0 || numericYear % 400 === 0);
  const days = [
    0,
    31,
    leap ? 29 : 28,
    31,
    30,
    31,
    30,
    31,
    31,
    30,
    31,
    30,
    31
  ][numericMonth] ?? 0;
  return numericMonth >= 1 && numericMonth <= 12 && Number(day) >= 1 && Number(day) <= days && Number(hour) <= 23 && Number(minute) <= 59 && Number(second) <= 60 && Number(offsetHour) <= 23 && Number(offsetMinute) <= 59;
}
function isIso8601Duration(value) {
  let index = value.startsWith("-") ? 1 : 0;
  if (value[index] !== "P")
    return false;
  index += 1;
  let inTime = false;
  let sawComponent = false;
  let sawTimeComponent = false;
  let lastDateOrder = -1;
  let lastTimeOrder = -1;
  let sawWeek = false;
  let sawOtherDateUnit = false;
  while (index < value.length) {
    if (value[index] === "T") {
      if (inTime || index + 1 === value.length)
        return false;
      inTime = true;
      index += 1;
      continue;
    }
    const digitStart = index;
    while (isAsciiDigit(value[index]))
      index += 1;
    if (index === digitStart)
      return false;
    let fractional = false;
    if (value[index] === ".") {
      fractional = true;
      index += 1;
      const fractionStart = index;
      while (isAsciiDigit(value[index]))
        index += 1;
      if (index === fractionStart)
        return false;
    }
    const unit = value[index];
    if (unit === undefined)
      return false;
    const order = inTime ? { H: 0, M: 1, S: 2 }[unit] : { Y: 0, M: 1, W: 2, D: 3 }[unit];
    if (order === undefined)
      return false;
    if (inTime) {
      if (order <= lastTimeOrder)
        return false;
      lastTimeOrder = order;
      sawTimeComponent = true;
    } else {
      if (order <= lastDateOrder)
        return false;
      lastDateOrder = order;
      if (unit === "W") {
        if (sawOtherDateUnit || index + 1 !== value.length)
          return false;
        sawWeek = true;
      } else {
        if (sawWeek)
          return false;
        sawOtherDateUnit = true;
      }
    }
    sawComponent = true;
    index += 1;
    if (fractional && index !== value.length)
      return false;
  }
  return sawComponent && (!inTime || sawTimeComponent);
}
function isAsciiDigit(value) {
  return value !== undefined && value >= "0" && value <= "9";
}
function schemaList(value) {
  if (!Array.isArray(value))
    return;
  return value.filter(isRecord2);
}
function isRecord2(value) {
  return typeof value === "object" && value !== null;
}
function isSchemaObject(value) {
  return isRecord2(value) && !Array.isArray(value);
}
export {
  validatePortableJson,
  validateResult,
  validateSchema
};
