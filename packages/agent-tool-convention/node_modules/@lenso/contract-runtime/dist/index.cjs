var __defProp = Object.defineProperty;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __hasOwnProp = Object.prototype.hasOwnProperty;
function __accessProp(key) {
  return this[key];
}
var __toCommonJS = (from) => {
  var entry = (__moduleCache ??= new WeakMap).get(from), desc;
  if (entry)
    return entry;
  entry = __defProp({}, "__esModule", { value: true });
  if (from && typeof from === "object" || typeof from === "function") {
    for (var key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(entry, key))
        __defProp(entry, key, {
          get: __accessProp.bind(from, key),
          enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
        });
  }
  __moduleCache.set(from, entry);
  return entry;
};
var __moduleCache;
var __returnValue = (v) => v;
function __exportSetter(name, newValue) {
  this[name] = __returnValue.bind(null, newValue);
}
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, {
      get: all[name],
      enumerable: true,
      configurable: true,
      set: __exportSetter.bind(all, name)
    });
};

// src/index.ts
var exports_src = {};
__export(exports_src, {
  decodeDomainError: () => decodeDomainError,
  decodePortableJson: () => decodePortableJson,
  encodePortableJson: () => encodePortableJson,
  isRuntimeFailureKind: () => isRuntimeFailureKind,
  portableValueProfile: () => portableValueProfile,
  runtimeFailureKinds: () => runtimeFailureKinds,
  validatePortableJson: () => validatePortableJson
});
module.exports = __toCommonJS(exports_src);
var runtimeFailureKinds = Object.freeze([
  "unavailable",
  "unknown_operation",
  "ambiguous_binding",
  "protocol_violation",
  "missing_plugin_factory",
  "unavailable_execution_class",
  "invalid_resolved_plan",
  "admission_closed",
  "resource_exhausted",
  "deadline_exceeded",
  "cancelled",
  "internal",
  "plugin_failure",
  "plugin_restart_exhausted"
]);
function isRuntimeFailureKind(value) {
  return runtimeFailureKinds.some((kind) => kind === value);
}
var portableValueProfile = {
  int64: "decimal-string",
  uint64: "decimal-string",
  bytes: "base64-string",
  timestamp: "RFC3339-string",
  duration: "ISO8601-string",
  missingAndNull: "distinct"
};
function encodePortableJson(value, subject) {
  validatePortableJson(value);
  const wire = JSON.stringify(value);
  if (wire === undefined)
    throw new Error(`${subject} cannot be encoded`);
  return wire;
}
function decodePortableJson(wire) {
  const value = JSON.parse(wire);
  validatePortableJson(value);
  return value;
}
function decodeDomainError(wire, knownStringCodes) {
  const value = decodePortableJson(wire);
  if (typeof value === "string") {
    if (knownStringCodes.includes(value))
      return value;
    return { code: value };
  }
  if (isRecord(value) && typeof value.code === "string")
    return value;
  throw new Error("Domain Error must be a string or object");
}
function validatePortableJson(value) {
  if (typeof value === "number") {
    if (!Number.isFinite(value) || Number.isInteger(value) && !Number.isSafeInteger(value)) {
      throw new Error("wire JSON contains an unsafe number");
    }
    return;
  }
  if (Array.isArray(value)) {
    for (const item of value)
      validatePortableJson(item);
    return;
  }
  if (isRecord(value)) {
    for (const item of Object.values(value))
      validatePortableJson(item);
  }
}
function isRecord(value) {
  return typeof value === "object" && value !== null;
}
