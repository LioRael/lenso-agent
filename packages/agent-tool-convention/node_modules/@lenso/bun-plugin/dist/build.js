// @bun
// src/build.ts
import { createHash } from "crypto";
var BUILD_API_VERSION = 1;
var MAX_GENERATED_FILES = 256;
var MAX_GENERATED_BYTES = 16 * 1024 * 1024;

class BuildOutputError extends Error {
  span;
  constructor(message, span) {
    super(message);
    this.name = "BuildOutputError";
    this.span = span;
  }
}
function validateBuildPackageMetadata(raw) {
  const span = EMPTY_SPAN;
  const metadata = record(raw, "lenso.build", span);
  const allowedKeys = new Set([
    "api_version",
    "export",
    "declarations",
    "handler_parameters",
    "provider_contracts"
  ]);
  const unknown = Object.keys(metadata).find((key) => !allowedKeys.has(key));
  if (unknown !== undefined) {
    throw new BuildOutputError(`lenso.build contains unknown field ${unknown}`, span);
  }
  for (const required of ["api_version", "export", "declarations", "provider_contracts"]) {
    if (!(required in metadata)) {
      throw new BuildOutputError(`lenso.build is missing field ${required}`, span);
    }
  }
  if (metadata.api_version !== BUILD_API_VERSION) {
    throw new BuildOutputError(`unsupported build metadata API ${String(metadata.api_version)}`, span);
  }
  const buildExport = packageExport(metadata.export, "lenso.build.export", span);
  const declarationsRecord = record(metadata.declarations, "lenso.build.declarations", span);
  const declarations = {};
  for (const [subpath, rawNames] of Object.entries(declarationsRecord)) {
    packageExport(subpath, "declaration subpath", span, true);
    const names = array(rawNames, `declarations.${subpath}`, span).map((name, index) => nonemptyString(name, `declarations.${subpath}[${index}]`, span));
    if (new Set(names).size !== names.length) {
      throw new BuildOutputError(`declarations.${subpath} contains a duplicate export`, span);
    }
    declarations[subpath] = Object.freeze(names);
  }
  const handlerParameters = {};
  if (metadata.handler_parameters !== undefined) {
    const subpaths = record(metadata.handler_parameters, "lenso.build.handler_parameters", span);
    for (const [subpath, rawExports] of Object.entries(subpaths)) {
      const declared = declarations[subpath];
      if (declared === undefined) {
        throw new BuildOutputError(`handler_parameters.${subpath} has no declared package subpath`, span);
      }
      const exports = record(rawExports, `handler_parameters.${subpath}`, span);
      const normalized = {};
      for (const [exportName, rawIndexes] of Object.entries(exports)) {
        if (!declared.includes(exportName)) {
          throw new BuildOutputError(`handler_parameters.${subpath}.${exportName} is not a declared export`, span);
        }
        const indexes = array(rawIndexes, `handler_parameters.${subpath}.${exportName}`, span).map((value, index) => nonnegativeInteger(value, `handler_parameters.${subpath}.${exportName}[${index}]`, span));
        if (new Set(indexes).size !== indexes.length) {
          throw new BuildOutputError(`handler_parameters.${subpath}.${exportName} contains a duplicate index`, span);
        }
        normalized[exportName] = Object.freeze(indexes);
      }
      handlerParameters[subpath] = Object.freeze(normalized);
    }
  }
  const providerContracts = array(metadata.provider_contracts, "lenso.build.provider_contracts", span).map((rawContract, index) => {
    const subject = `provider_contracts[${index}]`;
    const contract = record(rawContract, subject, span);
    exactKeys(contract, [
      "capability_id",
      "descriptor_version",
      "descriptor_digest",
      "request_operations"
    ], subject, span);
    const operations = array(contract.request_operations, `${subject}.request_operations`, span).map((operation, operationIndex) => nonemptyString(operation, `${subject}.request_operations[${operationIndex}]`, span));
    if (operations.length === 0 || new Set(operations).size !== operations.length) {
      throw new BuildOutputError(`${subject}.request_operations must be a nonempty unique list`, span);
    }
    return Object.freeze({
      capability_id: nonemptyString(contract.capability_id, `${subject}.capability_id`, span),
      descriptor_version: nonemptyString(contract.descriptor_version, `${subject}.descriptor_version`, span),
      descriptor_digest: canonicalDigest(contract.descriptor_digest, `${subject}.descriptor_digest`, span),
      request_operations: Object.freeze(operations)
    });
  });
  return Object.freeze({
    api_version: BUILD_API_VERSION,
    export: buildExport,
    declarations: Object.freeze(declarations),
    ...metadata.handler_parameters === undefined ? {} : { handler_parameters: Object.freeze(handlerParameters) },
    provider_contracts: Object.freeze(providerContracts)
  });
}
async function runLowering(lower, input, constraints) {
  if (input.api_version !== BUILD_API_VERSION) {
    throw new BuildOutputError(`unsupported build input API ${String(input.api_version)}`, input.span);
  }
  const raw = await lower(input);
  const output = validateLoweringOutput(raw, input);
  validateAllowedProviders(output.providers, constraints.allowedProviders, input.span);
  return output;
}
function fingerprintBuildInputs(input) {
  const canonical = {
    source_closure: canonicalFiles(input.sourceClosure, "sourceClosure"),
    contract_artifacts: canonicalFiles(input.contractArtifacts, "contractArtifacts"),
    locked_packages: [...input.lockedPackages].map((value) => ({
      name: nonemptyString(value.name, "locked package name", EMPTY_SPAN),
      version: nonemptyString(value.version, "locked package version", EMPTY_SPAN),
      integrity: nonemptyString(value.integrity, "locked package integrity", EMPTY_SPAN)
    })).sort((left, right) => `${left.name}\x00${left.version}\x00${left.integrity}`.localeCompare(`${right.name}\x00${right.version}\x00${right.integrity}`)),
    target: nonemptyString(input.target, "build target", EMPTY_SPAN)
  };
  return `sha256:${createHash("sha256").update(JSON.stringify(canonical)).digest("hex")}`;
}
function verifyBuildFingerprint(expected, input) {
  const actual = fingerprintBuildInputs(input);
  if (actual !== expected) {
    throw new BuildOutputError(`declaration/artifact fingerprint drift: expected ${expected}, received ${actual}`, EMPTY_SPAN);
  }
}
function validateLoweringOutput(raw, input) {
  const output = record(raw, "lowering output", input.span);
  exactKeys(output, ["api_version", "providers", "files", "diagnostics"], "lowering output", input.span);
  if (output.api_version !== BUILD_API_VERSION) {
    throw new BuildOutputError(`unsupported build output API ${String(output.api_version)}`, input.span);
  }
  const providers = array(output.providers, "providers", input.span).map((value, index) => validateProvider(value, index, input.span));
  const files = validateFiles(output.files, input.span);
  const diagnostics = array(output.diagnostics, "diagnostics", input.span).map((value, index) => validateDiagnostic(value, index, input.span));
  const providerIds = new Set;
  for (const provider of providers) {
    const identity = `${provider.capability_id}\x00${provider.descriptor_version}`;
    if (providerIds.has(identity)) {
      throw new BuildOutputError(`duplicate generated provider ${provider.capability_id} ${provider.descriptor_version}`, input.span);
    }
    providerIds.add(identity);
  }
  const expectedHandlers = collectHandlerReferences(input.arguments);
  const usedHandlers = new Set;
  for (const provider of providers) {
    for (const reference of provider.handler_references) {
      if (!expectedHandlers.has(reference)) {
        throw new BuildOutputError(`lowering used unknown handler reference ${reference}`, input.span);
      }
      if (usedHandlers.has(reference)) {
        throw new BuildOutputError(`lowering used handler reference ${reference} more than once`, input.span);
      }
      usedHandlers.add(reference);
    }
  }
  for (const reference of expectedHandlers) {
    if (!usedHandlers.has(reference)) {
      throw new BuildOutputError(`lowering did not use handler reference ${reference}`, input.span);
    }
  }
  return Object.freeze({
    api_version: BUILD_API_VERSION,
    providers: Object.freeze(providers),
    files: Object.freeze(files),
    diagnostics: Object.freeze(diagnostics)
  });
}
function validateProvider(raw, index, span) {
  const subject = `providers[${index}]`;
  const provider = record(raw, subject, span);
  exactKeys(provider, [
    "capability_id",
    "descriptor_version",
    "descriptor_digest",
    "binder",
    "handler_references"
  ], subject, span);
  const binder = record(provider.binder, `${subject}.binder`, span);
  exactKeys(binder, ["module", "export_name"], `${subject}.binder`, span);
  return Object.freeze({
    capability_id: nonemptyString(provider.capability_id, `${subject}.capability_id`, span),
    descriptor_version: nonemptyString(provider.descriptor_version, `${subject}.descriptor_version`, span),
    descriptor_digest: canonicalDigest(provider.descriptor_digest, `${subject}.descriptor_digest`, span),
    binder: Object.freeze({
      module: safeRelativePath(binder.module, `${subject}.binder.module`, span),
      export_name: nonemptyString(binder.export_name, `${subject}.binder.export_name`, span)
    }),
    handler_references: Object.freeze(array(provider.handler_references, `${subject}.handler_references`, span).map((value, handlerIndex) => nonemptyString(value, `${subject}.handler_references[${handlerIndex}]`, span)))
  });
}
function validateAllowedProviders(providers, allowed, span) {
  const identities = new Set(allowed.map((provider) => {
    const capability = nonemptyString(provider.capability_id, "allowed provider capability_id", span);
    const version = nonemptyString(provider.descriptor_version, "allowed provider descriptor_version", span);
    const digest = canonicalDigest(provider.descriptor_digest, "allowed provider descriptor_digest", span);
    return `${capability}\x00${version}\x00${digest}`;
  }));
  for (const provider of providers) {
    const identity = `${provider.capability_id}\x00${provider.descriptor_version}\x00${provider.descriptor_digest}`;
    if (!identities.has(identity)) {
      throw new BuildOutputError(`generated provider ${provider.capability_id} does not match an allowed exact contract`, span);
    }
  }
}
var EMPTY_SPAN = Object.freeze({ file: "<build>", start: 0, end: 0 });
function canonicalFiles(files, subject) {
  const seen = new Set;
  return [...files].map((file) => {
    const path = safeRelativePath(file.path, `${subject} path`, EMPTY_SPAN);
    if (seen.has(path)) {
      throw new BuildOutputError(`${subject} contains duplicate path ${path}`, EMPTY_SPAN);
    }
    seen.add(path);
    if (!/^sha256:[0-9a-f]{64}$/u.test(file.sha256)) {
      throw new BuildOutputError(`${subject} digest must be canonical SHA-256`, EMPTY_SPAN);
    }
    return { path, sha256: file.sha256 };
  }).sort((left, right) => left.path.localeCompare(right.path));
}
function validateFiles(raw, span) {
  const values = array(raw, "files", span);
  if (values.length > MAX_GENERATED_FILES) {
    throw new BuildOutputError(`lowering generated ${values.length} files; limit is ${MAX_GENERATED_FILES}`, span);
  }
  const paths = new Set;
  let totalBytes = 0;
  return values.map((value, index) => {
    const subject = `files[${index}]`;
    const file = record(value, subject, span);
    exactKeys(file, ["path", "contents"], subject, span);
    const path = safeRelativePath(file.path, `${subject}.path`, span);
    if (paths.has(path)) {
      throw new BuildOutputError(`duplicate generated path ${path}`, span);
    }
    paths.add(path);
    const contents = string(file.contents, `${subject}.contents`, span);
    totalBytes += new TextEncoder().encode(contents).byteLength;
    if (totalBytes > MAX_GENERATED_BYTES) {
      throw new BuildOutputError(`generated files exceed ${MAX_GENERATED_BYTES} bytes`, span);
    }
    return Object.freeze({ path, contents });
  });
}
function validateDiagnostic(raw, index, fallbackSpan) {
  const subject = `diagnostics[${index}]`;
  const diagnostic = record(raw, subject, fallbackSpan);
  exactKeys(diagnostic, ["severity", "message", "span"], subject, fallbackSpan);
  if (diagnostic.severity !== "error" && diagnostic.severity !== "warning") {
    throw new BuildOutputError(`${subject}.severity is invalid`, fallbackSpan);
  }
  return Object.freeze({
    severity: diagnostic.severity,
    message: nonemptyString(diagnostic.message, `${subject}.message`, fallbackSpan),
    span: validateSpan(diagnostic.span, `${subject}.span`, fallbackSpan)
  });
}
function collectHandlerReferences(arguments_, references = new Set) {
  for (const argument of arguments_) {
    if (argument.kind === "handler")
      references.add(argument.reference);
    if (argument.kind === "declaration") {
      collectHandlerReferences(argument.arguments, references);
    }
    if (argument.kind === "value") {
      collectHandlerValue(argument.value, references);
    }
  }
  return references;
}
function collectHandlerValue(value, references) {
  if (Array.isArray(value)) {
    for (const child of value)
      collectHandlerArgument(child, references);
    return;
  }
  if (typeof value !== "object" || value === null)
    return;
  for (const child of Object.values(value))
    collectHandlerArgument(child, references);
}
function collectHandlerArgument(value, references) {
  if (typeof value === "object" && value !== null && "kind" in value && "span" in value) {
    collectHandlerReferences([value], references);
  } else {
    collectHandlerValue(value, references);
  }
}
function validateSpan(raw, subject, fallbackSpan) {
  const span = record(raw, subject, fallbackSpan);
  exactKeys(span, ["file", "start", "end"], subject, fallbackSpan);
  const file = nonemptyString(span.file, `${subject}.file`, fallbackSpan);
  const start = nonnegativeInteger(span.start, `${subject}.start`, fallbackSpan);
  const end = nonnegativeInteger(span.end, `${subject}.end`, fallbackSpan);
  if (end < start)
    throw new BuildOutputError(`${subject}.end precedes start`, fallbackSpan);
  return Object.freeze({ file, start, end });
}
function safeRelativePath(raw, subject, span) {
  const path = nonemptyString(raw, subject, span);
  if (path.startsWith("/") || path.startsWith("\\") || /^[A-Za-z]:[\\/]/u.test(path) || path.split(/[\\/]/u).some((part) => part === ".." || part.length === 0)) {
    throw new BuildOutputError(`${subject} must be a normalized relative path`, span);
  }
  return path.replaceAll("\\", "/");
}
function packageExport(raw, subject, span, allowRoot = false) {
  const value = nonemptyString(raw, subject, span);
  if (allowRoot && value === "." || /^\.\/[A-Za-z0-9][A-Za-z0-9._/-]*$/u.test(value)) {
    if (!value.split("/").some((part) => part === ".." || part.length === 0)) {
      return value;
    }
  }
  throw new BuildOutputError(`${subject} must be a static package export subpath`, span);
}
function exactKeys(value, expected, subject, span) {
  const allowed = new Set(expected);
  const unknown = Object.keys(value).find((key) => !allowed.has(key));
  if (unknown !== undefined) {
    throw new BuildOutputError(`${subject} contains unknown field ${unknown}`, span);
  }
  const missing = expected.find((key) => !(key in value));
  if (missing !== undefined) {
    throw new BuildOutputError(`${subject} is missing field ${missing}`, span);
  }
}
function record(value, subject, span) {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    throw new BuildOutputError(`${subject} must be an object`, span);
  }
  return value;
}
function array(value, subject, span) {
  if (!Array.isArray(value))
    throw new BuildOutputError(`${subject} must be an array`, span);
  return value;
}
function string(value, subject, span) {
  if (typeof value !== "string")
    throw new BuildOutputError(`${subject} must be a string`, span);
  return value;
}
function nonemptyString(value, subject, span) {
  const result = string(value, subject, span);
  if (result.length === 0)
    throw new BuildOutputError(`${subject} must not be empty`, span);
  return result;
}
function canonicalDigest(value, subject, span) {
  const result = nonemptyString(value, subject, span);
  if (!/^sha256:[0-9a-f]{64}$/u.test(result)) {
    throw new BuildOutputError(`${subject} must be a canonical SHA-256 digest`, span);
  }
  return result;
}
function nonnegativeInteger(value, subject, span) {
  if (!Number.isSafeInteger(value) || value < 0) {
    throw new BuildOutputError(`${subject} must be a nonnegative safe integer`, span);
  }
  return value;
}
export {
  verifyBuildFingerprint,
  validateLoweringOutput,
  validateBuildPackageMetadata,
  runLowering,
  fingerprintBuildInputs,
  MAX_GENERATED_FILES,
  MAX_GENERATED_BYTES,
  BuildOutputError,
  BUILD_API_VERSION
};
