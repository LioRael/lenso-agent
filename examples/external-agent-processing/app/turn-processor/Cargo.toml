[package]
name = "external-turn-processor"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.lenso]
plugin-id = "example.turn-processor"
root-slot = "turn-processors"

[dependencies]
lenso = "0.5.21"
lenso-capability-agent-turn-processing = "0.1.0"
lenso-kernel = "0.3.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
