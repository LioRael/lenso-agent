[package]
name = "external-agent-caller"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.lenso]
plugin-id = "example.agent-caller"
root-slot = "agent-callers"

[dependencies]
lenso = "0.5.21"
lenso-capability-agent = "0.1.0"

[workspace]
