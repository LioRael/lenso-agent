[package]
name = "external-state-task-provider"
version = "0.1.0"
edition = "2024"
publish = false

[package.metadata.lenso]
plugin-id = "example.state-task-provider"
root-slot = "state-task-providers"

[dependencies]
lenso = "0.5.21"
lenso-capability-agent-durable-task = "0.1.0"
lenso-capability-agent-extension-state = "0.1.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
