name: Release Rust Tool SDK

on:
  pull_request:
    paths:
      - ".github/workflows/release-tool-sdk-crates.yml"
  workflow_dispatch:
    inputs:
      version:
        description: Exact SDK and macro version
        required: true
        type: string
      publish:
        description: Publish both packages to crates.io
        required: true
        default: false
        type: boolean

permissions: {}

concurrency:
  group: release-rust-tool-sdk
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88
        with:
          toolchain: nightly-2026-07-10
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
      - name: Validate release versions
        env:
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          python3 - <<'PYTHON'
          import os, pathlib, tomllib
          sdk = tomllib.loads(pathlib.Path("crates/lenso-agent-tool-sdk/Cargo.toml").read_text())
          macros = tomllib.loads(pathlib.Path("crates/lenso-agent-tool-sdk-macros/Cargo.toml").read_text())
          version = sdk["package"]["version"]
          assert version == macros["package"]["version"]
          assert version == sdk["dependencies"]["lenso-agent-tool-sdk-macros"]["version"]
          requested = os.environ["REQUESTED_VERSION"]
          if os.environ["GITHUB_EVENT_NAME"] == "workflow_dispatch":
              assert requested == version, "Requested version does not match manifests"
              assert os.environ["GITHUB_REF"] == "refs/heads/main", "Release must run from main"
          PYTHON
      - name: Test Tool authoring
        run: cargo test --locked -p lenso-agent-tool-sdk -p lenso-agent-tool-sdk-macros
      - name: Verify publishable packages
        run: cargo publish --dry-run -p lenso-agent-tool-sdk-macros -p lenso-agent-tool-sdk

  publish:
    if: github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main' && inputs.publish
    needs: verify
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@fa04a1451ff1842e2626ccb99004d0195b455a88
        with:
          toolchain: nightly-2026-07-10
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32
      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18
      - name: Publish in dependency order
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
        run: cargo publish -p lenso-agent-tool-sdk-macros -p lenso-agent-tool-sdk
