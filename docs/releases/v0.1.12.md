# Lenso Agent v0.1.12

Web startup now survives a model catalog outage when periodic refresh is enabled.
An expired cache remains unavailable for model admission, while the recovery UI
stays running and the Provider retries acquisition in the background. Model
selection recovers after successful acquisition without restarting the App.

Cache identity validation and maximum age remain unchanged. Configurations with
periodic refresh disabled retain synchronous acquisition failure behavior.
